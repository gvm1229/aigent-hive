# Synthetic test fixture

This 0.9.1 integrity repository exists only for local updater tests and must never be published,
tagged, promoted, or used as a release candidate. All identities, digests, signatures, and signing
evidence are synthetic public test values. Private signing material is intentionally absent.
