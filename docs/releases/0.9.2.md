# Aigent Hive 0.9.2

## 범위

- Source workspace 전용 usage guard를 제거하고 설치된 Hive의 전역 usage guard를 단일 정본으로 사용
- Consumer project의 project-local threshold는 Hive project setup을 마친 project에서만 허용
- Non-Hive project의 usage guard 호출·threshold 변경·중단은 적용 대상 제외
- 공개 README·한국어 README·설치 안내·npm README·plugin metadata를 `0.9.2`로 동기화

## 호환성

- `0.9.1` 설정·지식·사용자 선호 보존
- Provider API·credential 경로 추가 없음
- Native orchestration·custom subagent 구현은 포함하지 않으며 `0.9.3` 승인 전 동결

## 검증

- 실제 공개 numbered test build의 clean install·upgrade·rollback·recovery 수용
- Windows fresh session의 usage guard·지식·preference 보존과 build date 표시 확인
- 5개 native target·npm 6개 package·SHA-256·GitHub attestation·npm provenance 확인
