# Aigent Hive 0.9.1

## 범위

- 미등록 project의 Wiki lint를 user-root canonical Markdown과 파생 shared index로 자동 폴백
- registered project·unregistered project·source workspace의 명시적 lint 분기
- 전역·project 지침과 `knowledge-capture`·`knowledge-maintain` Skill 투영 동기화
- 공개 README의 stable 설치·시험판 표기와 source release date 정정

## 호환성

- `0.9.0` 설정·지식·사용자 선호 보존
- Provider API·credential 경로 추가 없음
- npm package family·직접 installer·지원 운영체제 범위 변경 없음

## 검증

- 미등록 project의 `hive knowledge lint --target <project-root> --user-root <user-root>` 자동 user-root 폴백
- user-root 직접 lint와 registered project lint PASS
- Windows 전역 preserving reinstall·`hive install --validate`·release date 표시
- 5개 native target·npm 6개 package·SHA-256·GitHub attestation·npm provenance
