# 현재 상태

- 작업 branch: `develop`, 벡터 통합 기준 `29279b21`
- 원격 통합: `origin/develop@29279b21`. 벡터 전용 branch의 검증된 변경을 병합·반영 완료
- product version: `0.10.0`
- 안정판 기준: `0.9.5`
- 마지막 공개 시험: `0.10.0-test.4`, 다음 번호: `0.10.0-test.6`
- 활성 계획: [PLAN.md](../plans/PLAN.md), [벡터 구현](../plans/active/hybrid-vector-search-0.10.0.md)
- 구현·수치 정본: [벡터 구현 계약](../architecture/vector-search.md), [제품 연결 재검증](../research/vector-product-integration-2026-08-28.md)

## 비벡터 완료 근거

- 한국어 활성 팩 반영·부정문 보호·손상 복구와 test.2 직접 갱신 수정 후 `develop` 병합
- 과거 기본 파일·사용자·외부 바이트 보존. macOS 임시 경로 수정 `0b4aa53e`, CI `33101026027` 통과
- test.4 소스 `5cedbed4`: 후보 `33101040467`, 게시 `33102125677`, 공개 설치 수용 `33102491627` 통과
- 실제 Windows x64·macOS arm64·Linux musl x64 수용. npm `latest=0.9.5` 유지
- 당시 전체 Rust·Clippy, Python 목록 710개 중 671 통과·39 조건부 건너뜀. 합산 근거는 보존 기록 참조
- 취소된 test.3 후보 `33100640263`: 미게시, 수용 근거 제외

## 현재 벡터 구현

- 고정 `sqlite-vec 0.1.9`·다국어 MiniLM·Python 3.12/3.13 선택형 보조 환경
- 정확한 설치 동의, 파일·실행 파일 지문 검사, 범위별 물리 색인·외부 승인 기록·불변 세대
- FTS 유지, 의미 질문의 점수 정규화 결합. 기본 사실 검색은 FTS
- 기밀 생성·조회 승인 분리와 일회성 소비, 게시 전후 권한·정본 재확인
- 중단 재개·이전 세대 복구·정상 중복 파일 정리. 오염·미확인 자료 자동 삭제 없음
- 같은 검색의 초기 RAG 연결 재사용과 공유 모음 16개씩 묶음 갱신. 새 SQLite 해시·권한·정본 검사 유지
- 기존 지식 Skill의 검색 선택·묶음 갱신 안내, test.2/test.4 직접 갱신 인증
- `0b268feb`: Windows 긴 DLL·모델 경로의 native 로더 표기 수정. SQLite·소유권 경로 불변
- `c3ab4901`: 실행 환경의 경로 계산 중복 제거. 전체 파일 목록·종류·해시 검사 유지
- `a4a2e46a`: Windows 혼합 CPU의 임베딩 계산 코어 종류 고정. 부모·전역 설정·사용자 CPU 제한 보존
- 실제 사용자 지식 변환·전역 벡터 설치 미실행. 시험 대상은 `tests/work/` 내부 합성 자료

## 최근 검증

- Windows 전체 Rust 892개 통과·수동 4개 제외. 관련 Clippy 통과, 원본 외부 라이브러리 unused 경고 6건 유지
- Python 전체 실행 당시 786개: 746 통과·40 조건부 건너뜀·실패/누락 0개. 새 수용 정책 검사 포함, 후속 수정은 아래 별도 검사
- 다섯 구간 시간: 문서 1.91초·보안 66.36초·계약 437.11초·통합 245.64초·출시 33.84초
- 건너뜀: Windows의 symlink 권한·POSIX 전용 동작·macOS 설치 시험·호스트별 기본 설정 조건. 다른 운영체제 통과로 간주 금지
- 목록 대조: `tests/work/scope-audit-20260828/vector-acceptance-reconciled-tests.json`
- 고정 관계 질문은 같은 시점의 사실로 검사. 직접 사실 30/30·관계 30/30, 현재/고정 사실 보존·오류/경고 0건
- 최신 실제 긴 경로·묶음 이동·3공유 모음·소스 81개 재개·취소/복구: 아래 세 운영체제 원격 수용 근거 참조
- 현재 바이너리 SHA `830479a3311bb0c57429507219f237f7c3bd75fa8511efcf899b1a389efb0ac0`, 제품 `e76be34b`. 숫자 문구 재검증 통과
- Windows 기밀 승인 분리·일회성 소비·공유 격리·실행기 손상 시 FTS 복귀 통과: `tests/work/vector-privacy-export-znq7xf8j/receipt.json` (`5fc08`)
- 실제 18모음·76문서: 묶음 생성·무변경·증분 2개·74개 벡터 보존 통과. 컴파일 경합의 시간은 성능 근거 제외
- 기존 기밀 승인·공유 검색 누출 0건·51,200개 기밀 조각의 승인 전 오류 노출 방지 근거는 재검증 문서 참조
- 원격 `33180339088`, 소스 `d9cb7733`: Windows·macOS·Linux 실제 벡터 수용 통과. 다음 공개 시험 수용과 구분
- `test.5` 후보 `33188952482`: 게시 전 취소. CI `33188953627`의 새 환경 시험 폴더 부재·Unix Clippy·문서 검사 문제 확인
- `96410afd`: 시험별 작업 폴더 준비와 Windows 오류 반환의 Unix 검사 정합화. Windows 벡터 Rust 24개·Python 23개 통과, Clippy·서식 통과. Unix 수정의 실제 확인은 다음 CI
- `ae48e988`: `묶음`·`마다` 문서 검사 오탐과 실제 종결 표현 수정. 문서 29개 중 28 통과·Windows symlink 권한 1개 제외
- 이전 macOS 설치 실패: 기본 Python의 SQLite 확장 미지원 확인, CI 호환 Python 선택으로 복구. 다음 소스 생성 실패 1건은 재현 원인 미확정으로 보존
- 숫자 문구 수정의 관련 Rust 628개·Python 19개·독립 검토 통과 후 위 전체 검사 재확인

## 채택 기준 현황

2026-08-29 유지보수자의 현재 스트레스 성능 수용 승인. [새 정책](../research/vector-acceptance-2026-08-29.md) 적용, 기존 기준 미달 기록은 보존.

| 대상 | 실제 결과와 한계 |
| --- | --- |
| 원본 의미 질문 | FTS 80% → 결합 96.7%. 순위 보호 뒤 정확 포함률 100%·평균 역순위 0.975 유지·손실 0개 |
| 독립 의미 질문 | FTS 46.7% → 결합 96.7%, 원본 평가와 별도 집계 |
| 정적 POTION | 원본 85%, 향상폭 5%p로 미달. 독립·대규모 후속 실행 없음 |
| E5-small 후속 | 원본 93.3%, 향상폭 13.3%p로 미달. 가중치·문항 재조정 없음 |
| 최신 100모음 재생성 | 5만 개 새 계산·벡터 바이트 일치. 638.597초로 새 900초 기준 통과, DB 전체 파일 바이트 동일성은 요구 범위에서 제외 |
| 최신 100개 증분 | 새 계산 100개·49,900개 보존. 벡터 51.989초로 새 90초 기준 통과, FTS 포함 70.419초 |
| 최신 저장량 | 색인·작업·제어·실패 보존 442,751,701B로 512MiB 통과. 실행 환경·독립 감사 복사본 별도 |
| 최신 번호 조회 | 120회 모두 기대 문서 인용·기존 FTS 1위 유지. 이전 누락 38건 수정, 실제 벡터 사용 60회 |
| 최신 전체 검색 | 전역 의미 p95 3.067초·한 모음 2.276초로 새 4초 기준 통과. FTS는 0.546/0.353초, 모델 유지 warm은 미측정·범위 제외 |
| 저장소 단독 조회 | 열린 100DB·저장 벡터 30개, 합산 SQL p95 98.876ms로 새 150ms 기준 통과. 모델·전체 CLI·질문 품질 근거 아님 |
| 순위 보호 30문서 조회 | 원본·독립 의미 질문의 새 CLI p95 1.796/1.820초. 두 평가 모두 58/60, 정확 30개 평균 역순위 0.975 유지 |

대체된 과거 조합·CPU 제한·단일 모음·Rust 부품 비교는 [제품 연결 재검증](../research/vector-product-integration-2026-08-28.md)에 보존.

과거 37.31ms는 같은 프로세스의 임베딩만 측정한 p95. 643.45ms는 모델 생성·첫 임베딩 한 번의 관측값.
둘 다 전체 검색 통과 근거나 cold p95로 사용 금지. 새 정책도 부품 측정과 전체 요청 시간 구분 유지.

## 남은 결정과 출시 조건

1. `VEC10-007` 채택 완료. 새 스트레스 정책으로 기존 실측 재판정, 품질·보안·보존·복구 기준 유지
2. 작업 단위 모델 유지는 성능 개선안이므로 이번 범위에서 생략. 현재 단발 호출·요청별 검증 유지
3. test.6 준비·게시·공개 설치 수용으로 진행. `VEC10-012`, `REL10-002–004`는 다음 단계

시각 설명: [벡터 검색 HTML](../hive-vector-search.ko.html). 정적 구조·링크·스크립트 검사 통과, 자동 화면 검증은 브라우저의 로컬 파일 정책으로 미실행.

추가 재정렬 모델·상주 서버 실행 없음. 안정판 `REL10-005–007`은 명시 제외.
버전명 포함 사용자 승인 전 `main` 통합·안정판 tag·게시·설치 금지.

과거 누적 근거: [개선된 대규모 측정 전 기록](../archive/state/0.10.0-vector-before-optimized-scale.md),
[한국어 재검증 전 기록](../archive/state/0.10.0-test.2-reopened.md)
