# 현재 상태

- 기준 branch: `feature/0.10.0@7f34552a`
- 원격 상태: 검증 후 push 예정
- product version: `0.10.0`
- 안정판 기준: `0.9.5`; 다음 공개 시험: `0.10.0-test.4`
- 활성 계획: [`PLAN.md`](../plans/PLAN.md)
- 현재 단계: `develop` 통합 뒤 macOS 경로 수정·교차 운영체제 재검증

## 이번 수정

- `720c8603`: 활성 한국어 팩을 검사·비교·hook에서 실제 사용, 규칙 구조·안전 상한 검증, 부정문 변경 거부
- 의미 보존 한계: 결정적 검사 통과만으로 동등성 확정 불가. 활성 host의 주장·주체·수치·출처 비교 필수
- 팩 손상 시 오류 반환, 이전 팩 재검증 후 복구, 최초 활성화의 내장 팩 복구, 재활성화 시 복구 지점 보존
- `08d34e60`: 공개 test.2의 정확한 이전 파일 두 개를 인증해 같은 제품 버전의 프로젝트 갱신 허용
- 과거 stable base byte 변경 없음. 혼합·위조 이전 파일 거부
- `7f34552a`: Copier의 한국어 Skill 해시를 현재 정본과 일치
- `e0c413ee`: 다음 시험판 메타데이터와 출시일 `2026-08-28` 정합화

## 현재 검증 근거

- Windows x64: 전체 Rust workspace `--all-targets --all-features --locked`, Clippy `-D warnings` 통과
- 한국어 집중 검사: Rust core 5건·CLI 5건·Python 12건 통과
- 공개 test.2 파일과 동결본 byte 일치, 실제 구 CLI→새 CLI의 격리 프로젝트·사용자 갱신 통과
- 세 host 프로젝트 갱신·외부 파일 보존 검사 통과. 실제 macOS·Linux 실행 근거로 사용 금지
- 독립 검토: 수정 후 차단 결함 0건
- Copier 비교 4건 통과. Python 전체 710개 목록 대조: 671 통과·39 건너뜀·누락 0건
- Windows 미지원 POSIX·macOS·심볼릭 링크 권한 등의 39개 시험은 해당 운영체제의 수용 근거 아님
- 실패 모듈 수정·재실행과 나머지 검사를 합친 근거. 단일 `--all` 실행 성공으로 표현 금지
- 원본 로그와 목록 대조: `tests/work/scope-audit-20260828/reconciled-tests.json`

## 기존 공개 시험과 한계

- `4467d212`: `feature/0.10.0`을 `develop`에 fast-forward 병합·양쪽 원격 반영
- CI `33100616646`의 macOS `/var` 경로 거부 확인. 기본 임시 경로만 정규화하는 후속 수정
- test.3 후보 `33100640263`: 게시 전 취소. 미게시 후보이며 공개 시험 수용 근거 없음

- 공개 `0.10.0-test.2`: candidate `32676134575`, publication `32676726910`, 세 운영체제 수용 `32677477765`
- 한국어 반례 세 건 확인으로 이전 전체 완료 판정 취소. 위 기록은 수정된 제품의 수용 근거 아님
- 이전 벡터 결과: 의미 검색 93.3%, FTS 78.3%, 고유 5만 건 전체 변환은 1천 건 측정 기반 추정으로 600초 기준 실패
- 새 벡터 연구: 격리 환경에서 고유 합성 문서 5만 건 실제 변환 진행. 제품 의존성·기능은 아직 미추가
- 세부 과거 기록: [재검증 전 상태](../archive/state/0.10.0-test.2-reopened.md)

## 다음 작업과 권한

1. 비벡터 전체 회귀 검사 완료 후 `develop` 병합·push
2. 수정 제품의 번호 공개 시험과 Windows x64·macOS arm64·Linux musl x64 수용
3. `feature/0.10.0-vector-search`에서 의미 검색 전용 구현·검증
4. 안정판 `REL10-005–007`: 명시 제외. 버전명 포함 사용자 승인 전 protected `main` 통합·tag·게시·설치 금지

현재 사용자 수동 장애 요인 없음. 남은 검사·병합·벡터 구현은 Agent 소유 작업.
