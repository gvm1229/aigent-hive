# 현재 상태

- 작업 branch: `feature/0.10.0-vector-search`, 소스 확인 기준 `ddb91859`
- 원격 통합 기준: `origin/develop@5cedbed4`. 벡터 변경은 전용 branch에서 검증 중이며 `develop` 미통합
- 제품 버전: `0.10.0`, 안정판 기준 `0.9.5`
- 마지막 공개 시험: `0.10.0-test.4`, 다음 번호: `0.10.0-test.5`
- 활성 계획: [PLAN.md](../plans/PLAN.md), [벡터 구현](../plans/active/hybrid-vector-search-0.10.0.md)
- 구현·수치 정본: [벡터 구현 계약](../architecture/vector-search.md), [제품 연결 재검증](../research/vector-product-integration-2026-08-28.md)

## 비벡터 완료 근거

- 한국어 활성 팩 반영·부정문 보호·손상 복구와 test.2 직접 갱신 수정 후 `develop` 병합
- 과거 기본 파일·사용자·외부 바이트 보존. macOS 임시 경로 수정 `0b4aa53e`, CI `33101026027` 통과
- test.4 소스 `5cedbed4`: 후보 `33101040467`, 게시 `33102125677`, 공개 설치 수용 `33102491627` 통과
- 실제 Windows x64·macOS arm64·Linux musl x64 수용. npm `latest=0.9.5` 유지
- 당시 전체 Rust·Clippy, Python 목록 710개 중 671 통과·39 조건부 건너뜀. 합산 근거는 보존 기록 참조
- 취소된 test.3 후보 `33100640263`: 미게시, 수용 근거 제외

## 현재 벡터 구현

- 고정 `sqlite-vec 0.1.9`·다국어 MiniLM·Python 3.12/3.13 선택형 보조 환경
- 정확한 설치 동의, 파일·실행 파일 지문 검사, 범위별 물리 색인·외부 승인 기록·불변 세대
- FTS 유지, 의미 질문의 점수 정규화 결합. 기본 사실 검색은 FTS
- 기밀 생성·조회 승인 분리와 일회성 소비, 게시 전후 권한·정본 재확인
- 중단 재개·이전 세대 복구·정상 중복 파일 정리. 오염·미확인 자료 자동 삭제 없음
- 같은 검색의 초기 RAG 연결 재사용과 공유 모음 16개씩 묶음 갱신. 새 SQLite 해시·권한·정본 검사 유지
- 기존 지식 Skill의 검색 선택·묶음 갱신 안내, test.2/test.4 직접 갱신 인증
- `0b268feb`: Windows 긴 DLL·모델 경로의 native 로더 표기 수정. SQLite·소유권 경로 불변
- `c3ab4901`: 실행 환경의 경로 계산 중복 제거. 전체 파일 목록·종류·해시 검사 유지
- `a4a2e46a`: Windows 혼합 CPU의 임베딩 계산 코어 종류 고정. 부모·전역 설정·사용자 CPU 제한 보존
- 실제 사용자 지식 변환·전역 벡터 설치 미실행. 시험 대상은 `tests/work/` 내부 합성 자료

## 최근 검증

- Windows 전체 Rust 877개 통과·수동 4개 제외, Clippy 통과. 원본 외부 라이브러리 unused 경고 6건 유지
- Python 현재 목록 783개: 743 통과·40 조건부 건너뜀·실패/누락 0개. 깨끗한 소스 `6e7c4a3c`에서 단일 전체 실행 완료
- 다섯 구간 시간: 문서 1.97초·보안 67.81초·계약 442.50초·통합 252.06초·출시 45.41초
- 건너뜀: Windows의 symlink 권한·POSIX 전용 동작·macOS 설치 시험·호스트별 기본 설정 조건. 다른 운영체제 통과로 간주 금지
- 목록 대조: `tests/work/scope-audit-20260828/vector-parallel-reconciled-tests.json`
- 고정 관계 질문은 같은 시점의 사실로 검사. 직접 사실 30/30·관계 30/30, 현재/고정 사실 보존·오류/경고 0건
- Windows 실제 긴 경로·8문서 묶음 이동·3공유 모음·소스 81개 재개·강제 취소/복구 통과: `tests/work/vector-native-lblw2nrc/receipt.json`
- 새 병렬 처리 바이너리 SHA `2707deaea7c2dc215f99dee8ec56a6bb9ef86e58a6adfed2d9a2d160b3106ac9`, 깨끗한 소스 `6e7c4a3c`. 대규모 속도는 별도
- 묶음/검색 재사용: Wiki 157+1+7+8·CLI 21·Clippy 통과. Skill 투영/과거 갱신 35개 통과·OS 전용 2개 제외
- 실제 18모음·76문서: 묶음 생성·무변경·증분 2개·74개 벡터 보존 통과. 컴파일 경합의 시간은 성능 근거 제외
- 기존 기밀 승인·공유 검색 누출 0건·51,200개 기밀 조각의 승인 전 오류 노출 방지 근거는 재검증 문서 참조
- 원격 `33160056855`, 소스 `41db2ddf`: 새 병렬 처리의 세 운영체제 실제 벡터 수용 통과. 공개 test.5 수용과 구분
- 이전 macOS 설치 실패: 기본 Python의 SQLite 확장 미지원 확인, CI 호환 Python 선택으로 복구. 다음 소스 생성 실패 1건은 재현 원인 미확정으로 보존
- CPU 보강: Python 실행 환경/부모/CLI 40개 중 39 통과·Unix 전용 1개 제외, Rust 벡터 17개·Clippy 통과. 실제 Windows 자식 affinity 변경·부모 보존 확인

## 채택 기준 현황

| 대상 | 실제 결과와 한계 |
| --- | --- |
| 원본 의미 질문 | FTS 80% → MiniLM 결합 96.7%, 정확 질문 100% 유지 |
| 독립 의미 질문 | FTS 46.7% → 결합 96.7%, 원본 평가와 별도 집계 |
| 정적 POTION | 원본 85%, 향상폭 5%p로 미달. 독립·대규모 후속 실행 없음 |
| E5-small 후속 | 원본 93.3%, 향상폭 13.3%p로 미달. 가중치·문항 재조정 없음 |
| 보조 실행기 한 모음 | 고유 5만 변환 488.76초·183.4MB. 100개 모음 CLI 통과 증거 아님 |
| 기존 실제 100개 모음 | 전체 629.27초, 변경 후 fresh 651.35초, 증분 100개 112.22초로 미달 |
| 기존 전체 검색 | 전역 의미 p95 3.413초·한 모음 2.643초. 전체 CLI 시간이며 500ms/2초 기준 미달 |
| 기존 재생성 동등성 | ID·digest 5만 개 일치, 벡터 바이트 47,999개 동일. 후속 동일 입력 시험에서 실행 P/E 코어에 따른 차이 재현 |
| 기존 누적 저장량 | 색인·중간·다운로드·제어 합계 1,026,291,938B로 미달. 실행 환경 별도 |
| 새 100개 모음 | 전체 643.58초로 시간 미달, 색인/관리정보 186,201,944B로 용량 통과. 이후 세대·증분 보존량은 별도 |
| 4×4 전체 재생성 | 532.67초·이전 세대 포함 372,159,088B 통과. 6,500개 벡터 수치 차이로 동등성 미달 |
| P 코어 고정 전체 생성 | 532.66초·372,159,088B 통과. 5만 개 모두 과거 두 결과 중 하나와 정확히 일치. 제품 CPU 수정 전 외부 제한 실험 |
| P 코어 증분 첫 시도 | 12×1 실행 67.47초·55개 게시·45개 잠금 시간 초과. 30초 미달. 실패분 복구 뒤 총 100개 게시·49,900개 재사용 확인 |
| 같은 모델 Rust 실험 | 질문 변환 구간 p95 0.960초, Python 1.608초·30쌍 오차 1e-6 이내. 전체 검색·500ms 통과 근거 아님, 제품 미도입 |

과거 37.31ms는 같은 프로세스의 임베딩만 측정한 p95. 643.45ms는 모델 생성·첫 임베딩 한 번의 관측값.
둘 다 전체 검색 통과 근거나 cold p95로 사용 금지. 현재 500ms/2초 기준 유지.

## 남은 실행 순서

1. 병렬 전체 생성 577.810초·5만 벡터 바이트 일치·누적 442.8MB 통과. 순차 전체 932.943초·증분 95.943초 실패 기록 보존
2. 새 조합의 후속 100개 증분과 같은 입력 재생성 측정. 최대 16작업·60초 구간, 미완료만 재개
3. 최종 전체 검색·100개 증분·재생성 동등성 확인. 500ms/2초·30초 기준 불변, 로컬 전체 검사 완료
4. 세 운영체제의 실제 벡터 설치·소스/지식 이동 수용과 최종 조합의 전체 검사
5. 통과한 조합만 다음 번호 시험판 게시·공개 설치 수용

추가 재정렬 모델·상주 서버 실행 없음. 안정판 `REL10-005–007`은 명시 제외.
버전명 포함 사용자 승인 전 `main` 통합·안정판 tag·게시·설치 금지.

과거 누적 근거: [개선된 대규모 측정 전 기록](../archive/state/0.10.0-vector-before-optimized-scale.md),
[한국어 재검증 전 기록](../archive/state/0.10.0-test.2-reopened.md)
