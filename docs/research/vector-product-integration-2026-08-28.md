# 벡터 제품 연결 재검증

## 판정

- 기능 연결과 최종 채택 판정 분리. MiniLM 고정 질문 품질·세 운영체제 기능 수용 통과, 최신 대규모 재생성 시간 미달
- 구현 범위: `0.10.0`, 전용 branch `feature/0.10.0-vector-search`
- 기존 FTS 유지. 실제 사용자 지식 변경·전역 벡터 활성화 미실행
- 모든 설치·모델·오염 시험은 `tests/work/` 내부의 합성 자료만 사용

## 최신 같은 입력 재생성

- 제품 `4e971a78`, 바이너리 `5fc08c6e…6f82265c`, 계약 `76aab2d3…f707fc`. Windows 합성 100모음·고유 5만 개, 16작업·60초 구간·10호출
- 신규 변환 50,000개·모음 100개 완료. 기존 active 재사용 없는 fresh와 이번 실행의 검증된 checkpoint 재개만 사용
- 최초 status 포함 638.597초로 600초 기준 미달. 목록 호출만 637.049초, 별도 사후 감사 25.648초. 정본/FTS 갱신·수동 정리 없음
- 벡터 5만 개 바이트 일치·최대 오차 0·보존 오류 0개. DB 전체 파일 SHA는 100개 모두 차이, 논리 입력과 벡터 동일성으로 검증
- 색인·작업·제어·실패 보존량 442,759,893B로 512MiB 통과. 실행 환경 1,502,753,490B와 독립 감사 복사본 557,972,128B는 별도
- 표본 기반 실행 트리 최대 RSS 4,699,529,216B·전용 메모리 5,529,546,752B, CPU 합계 하한 6,873.938초. 공유 페이지 중복과 짧은 프로세스 누락 가능
- 과거 `2707` 전체 생성 577.810초 통과는 보존하되 최신 fresh 결과의 통과 근거로 대체 금지. 두 실행 사이 원인 통제 비교는 미실행
- 최신 세 운영체제 기능 수용: `33174134490`, 소스 `6deb8676`, Windows 61호출·macOS/Linux 각 60호출 통과. 공개 test.5 수용과 구분
- 원본: `tests/work/scope-audit-20260828/vector-scale-100/optimized-100/shared-list-next/wave4-next/final-acceptance/fresh50k-measurement/summary.json`, 현재 제어 기준 `after-control-baseline.json`

## 최신 증분과 후속 제안

### 숫자가 포함된 짧은 문구의 정확 조회 보호

- 최신 5만 자료·120회 조회: `자료 00500` 형식에서 FTS는 각 범위 30/30 인용, 의미 결합은 전역 3/30·한 모음 19/30. 원본 실패 38건 보존
- 원인: 현재 정확 조회 분류는 공백 없는 식별자 또는 세 단어 이상 문구를 보호하지만, 이름과 번호로 된 두 단어 문구는 제외
- 수정 후보: 숫자 또는 숫자 포함 구조 식별자가 있는 문구는 전체 문자열의 일치와 좌우 경계를 검사한 뒤 기존 FTS 순서 보호
- `자료 00500`과 `자료 005000`·부호·소수·비율·경로 연장을 구분. 일반 두 개념어 문구는 기존처럼 의미 결합 유지
- 질문·정답·가중치·모델·실행 계약 변경 없음. 짧은 숫자 문구와 경계 반례 → 관련 Rust·Python·독립 검토 → 같은 고정 질문 재측정
- 이 수정은 속도 미달의 해결 근거가 아님. 원본/독립 품질과 대규모 인용 확인을 별도 수행하고, 작업 단위 모델 유지 제안은 계속 미승인
- 구현 `e76be34b`: 관련 Rust 628개·Python 19개 통과·수동 4개 제외, Clippy 자체 경고 0개·독립 검토 통과
- 새 숫자 문구만 공백·개행 정규화. 기존 scalar·별칭·명시 인용 유지. `release alpha-beta`는 원래 검색 토큰 세 개로 보호되던 동작을 그대로 보존
- 수정 전 실제 120회 p95: 전역 FTS 0.538085초·한 모음 FTS 0.334935초·전역 의미 3.039111초·한 모음 의미 2.262637초. 의미 모드는 실제 벡터 사용 각 30회, 인용 실패 27/11건
- 원본: `final-acceptance/query120/measurement/summary.json`, SHA `b958c81e…93ca823`. 질문·실패 응답·시간 보존, 모델 유지 warm 500ms와 engine 50ms는 별도 미측정

### 최신 100개 변경 갱신

- 같은 `5fc08c6e…6f82265c`·`76aab2d3…f707fc`, fresh 완료 뒤 각 모음에 새 문단 1개씩 정확히 100개 적용. 기존 문단의 재적용 없음
- 벡터 1호출·7구간·100모음 게시·100개 새 계산·49,900개 이전 벡터 바이트 보존, 보존 오류 0개
- 벡터 51.989초로 30초 기준 미달. FTS 1회 18.430초, 합계 70.419초, 최초 쓰기부터 전체 71.743초
- 제품 구간 합계: 실행기 27.263초·정본 준비 4.873초·사전 준비 4.251초·작업 파일 1.165초·게시 13.551초. 실행기에는 시작·전송·결과 처리가 포함되며 모델 계산만의 시간이 아님
- DB별 실행 시간 합계 94.813초는 서로 겹치는 값. 전체 CLI 시간에서 빼서 추가 비용으로 해석 금지
- 저장량 442,751,701B로 512MiB 통과. 앞선 fresh 638.597초 실패·기존 실패 자료·실행 환경 보존
- 새 독립 참조 `reference-after-incremental.json`, SHA `d42d2f99…bac68`. 실제 현재 DB와 제어 기록 200개 일반 복사, 186,202,784B
- 원본: `tests/work/scope-audit-20260828/vector-scale-100/optimized-100/shared-list-next/wave4-next/final-acceptance/incremental-next/incremental100-measurement/summary.json`, `phase-observations.json`

### 작업 단위 모델 유지 제안 — 미승인

- 현재 구현의 전체 검색·갱신 시간 기준 미달에 대한 후속 설계 후보. 현재 제품·실행 계약 변경 없음
- 후보: 유한한 한 작업의 여러 조회·갱신 중에만 모델 유지, 완료·취소·기한 종료 시 보조 프로세스 종료
- 금지 유지: 백그라운드 서버·자동 시작·질의 기록·제공자 API·사용자 전역 설정 변경
- 각 요청의 새 권한·정본·등록부·SQLite 무결성 검사 유지. FTS 준비 상태의 요청 간 재사용 승인으로 해석 금지
- 메모리 점유 시간과 호출 방식 변경에 대한 유지보수자 명시 동의 대기. 현재 조합의 최종 검색 측정은 독립적으로 지속
- DB 동시 수 4→16 후보도 미채택. 균등한 소량 변경의 유휴 예산 감소 가능성과 큰 모음 하나를 포함한 혼합 부하의 작업 수 감소를 함께 비교해야 하는 조건

## 초기 제품 경로

- `84618f95`: 승인형 실행 환경·범위별 색인·중단 재개·의미 검색 연결
- Python 실행 전 파일 해시 검사, 고정 패키지·모델 검증, SQLite 밖 승인 기록과 DB 해시 결합
- Windows 특수 경로의 SQLite 주소 오류 수정. 일반 경로 변환 뒤 원래 루트와 파일 정체성 동등성 확인
- 당시 안전성 바이너리 SHA-256: `38bbdbe661582945abe19adf76922cf69bbeed56733df449d4872f5a45cbf0e4`
- 실제 다중 DB 검색, 검색 도중 비활성화, DB 손상, 보조 코드 변조, 등록부 변경, 공개 조회의 비공개 자료 배제, 명시적 비공개 조회 통과
- 실제 소스 검색도 같은 바이너리에서 벡터 후보 5개 반환 확인
- `ae7ee170`·`4e9f1baf`: 기밀 생성·조회 승인 분리, 승인 일회성 소비, 모음별 최신성 및 대상·현재 프로젝트 권한의 게시 전후 검사
- 실제 기밀 조회·공유 검색 유출 0건·승인 없는 생성 거부·원본 보존 확인. 바이너리 SHA-256 `2cfb428331c17cb425af361f4d06a2f4236f94158b6c2b32d4b30401a37b056b`
- 51,200개 기밀 조각에서 크기 제한 오류가 승인 전에 노출되지 않는 별도 회귀 시험 통과, 44.65초
- 조회의 정본 검증은 실제 반환 항목에 한정. 새 문서 발견·외부 전체 수정 반영은 기존 FTS 갱신과 벡터 재생성 필요
- 과거 버전 갱신·다른 운영체제 수용·누적 저장량 관리는 별도 미완료 범위

## 초기 RRF 품질 비교

자료: 고정 문서 30개를 실제 Wiki로 변환, 질문 120개. 의미 질문은 표현 변형 40개와 교차 언어 20개.
정확한 식별자·수치 질문 30개와 관계 질문 30개는 별도 집계.
정답 문서가 상위 10개에 포함된 질문 비율 사용.

| 경로 | 의미 질문 정답률 | 정확 질문 정답률 | 해석 |
| --- | ---: | ---: | --- |
| 실제 FTS | 80.0% | 100% | 비교 기준 |
| 실제 MiniLM·FTS 결합 | 90.0% | 100% | 향상폭 10%p, 요구 15%p 미달 |
| MiniLM 벡터 단독 탐색 | 91.7% | 93.3% | 직접 사실 검색 대체에 부적합 |
| E5 small·FTS 결합 탐색 | 88.3% | 100% | 이번 자료에서 개선 없음 |
| E5 base·FTS 결합 탐색 | 86.7% | 100% | 더 큰 모델도 기준 미달 |

- 당시 제품 결합: 같은 정본 청크와 현재 권한 검증, RRF 상수 60
- 탐색 비교: 같은 실제 청크에 대한 벡터 점수와 CLI의 FTS 순위 사용
- 당시 순위 결합 가중치·상수·교차 배치 비교도 채택 기준 미달. 이후 점수 결합 검증과 구분
- 모델·질문 수가 작은 탐색 결과. 일반적인 모델 우열이나 다른 자료의 성능으로 확대 해석 금지
- 최초 개발용 CLI의 의미 질문 cold p95: 2.071초. 2초 기준 미달이며 배포 빌드 최종 수용 근거 아님
- 최초 120개 측정의 시작 시점 바이너리 해시 기록 결손. 최종 고정 바이너리의 재측정 필수
- 단독 문서 30개·원본 120개·독립 60개 재측정: 바이너리 `ba66021215ae6e734b7300aad7f60e56f6552f5fc1bb9799e3e13e1a6ea9b4d2`
- 원본 의미 80→90%, 정확 질문 100→100%, 독립 의미 46.7→96.7%. 원본 의미 cold p95 2.005초로 2초 기준 미달
- `release-vector-queries-*-isolated.json` 사용. 다른 시험 collection을 함께 둔 이전 측정은 FTS 통계의 영향을 받을 수 있어 별도 기록으로 유지

## 추가 모델 후보

### 점수 결합의 실제 제품 검증

- `1d70e20e`: 질문별 점수 정규화, 벡터 0.75·FTS 0.25 결합. source·consumer 모두 같은 방식 사용
- 다른 모음이 없는 Wiki 30개에서 원본 120문항과 독립 60문항을 실제 CLI로 각각 실행
- 원본 의미 질문: FTS 80.0% → 결합 96.7%, 향상폭 16.7%p. 정확 질문 100% → 100%
- 독립 의미 질문: FTS 46.7% → 결합 96.7%. 두 평가의 문항·정답·채택 기준은 변경하지 않음
- 원본 의미 질문 cold p95 1.948초, 독립 1.971초. 작은 자료의 실제 CLI 결과이며 5만 청크 속도 근거 아님
- 측정 바이너리: `39842b7ad7de59ce087297ae35b8ce83d0c6ac34e5521ed5c3b776d4e999b91e`
- 원본 자료: `score-vector-queries-original-isolated.json`, `score-vector-queries-holdout-isolated.json`
- `0cc367aa`: 승인 환경 검사와 조회를 같은 프로세스에서 실행. 이 후속 변경의 실제 전체 시간은 별도 재측정

### 공유 CPU 실행의 전체 변환

- 같은 모델·본문·고유 5만 청크에서 공유 CPU 12개 동시 작업: 실제 전체 변환 488.76초
- 완성 DB 91,705,344 bytes, 최신 중간 복사본 포함 183,410,688 bytes
- 8개 동시 작업은 605.25초로 미달. 지원 CPU 수와 12 중 작은 수를 기본으로 사용하며 실제 실행 예산을 승인에 고정
- `shared-vector-pipeline-50000-12.json`: 600초·512MiB 기준 통과는 보조 실행기 한 물리 모음에만 해당
- 정본 검사·100개 모음 운영을 포함한 실제 CLI 전체 시간, 증분과 세 운영체제는 별도 검증 대상

### 100개 모음의 실제 CLI 측정

- 고정 바이너리 `5207bf9109e43f9918e3f01777dc5c4952fce612a4119447fb266f8e52a9d19a`, 실제 공유 모음 100개×500문서
- 동시에 CLI 4개·각 CPU 작업 3개: 전체 629.27초, 변환 50,000개. 600초 기준은 29.27초 초과
- 사전 고정한 각 모음 1문서씩 변경: 실제 추가 변환 100개·112.22초. 30초 기준 미달
- 증분 전 기존 FTS 갱신은 별도 18.69초. 한 모음에 모인 100개 변경의 과거 12.87초와 다른 운영 비용
- 정본 보존과 등록부·FTS의 벡터 단계 무변경 확인. 저장량은 모델·실행 환경과 모든 색인·중간 파일을 나누어 집계 중
- 후속 후보: CLI 3개×작업 4개로 64개 단위 8개 묶음의 유휴 시간을 줄이는 비교. 현재 결과를 덮어쓰거나 기준을 완화하지 않음
- 재생성 동등성·400회 검색·후속 실행 구성을 아직 최종 수용으로 표시하지 않음
- 실제 재생성의 문서 ID·digest는 5만 개 모두 같지만 벡터 바이트는 47,999개만 동일. 사전 오차 1e-6에서 6개 모음 실패, 최대 차이 약 0.000149
- 다음 탐색: 같은 고정 모델의 ONNX 계산 묶음 1·4·8·16·64개 비교. 입력 조합에 따른 양자화 영향과 처리 시간을 함께 검사하며 문항·본문·모델·기준은 유지
- 용량 보강 대상: 정상 설치 뒤 남은 인증 다운로드 복사본, 정상 게시 뒤 불필요한 staging, 정상 fresh 작업에서 쌓이는 중복 quarantine. 오염·미확인 자료는 자동 삭제하지 않음
- 보강 전 실제 저장량: active·previous·staging·quarantine 각각 185,913,344B, 다운로드 잔여 282,306,162B, 제어 기록 332,400B
- 실행 환경 별도 375,684,329B. 512MiB 기준 대상은 1,026,291,938B로 실패, 모든 원본 유지
- `c59c860a`·`94c833cf` 보강 뒤 Windows 새 설치·무변경 갱신·fresh·rollback: 해당 다운로드·staging·quarantine 잔여 0건
- `tests/work/vector-native-2ib1eray/receipt.json`의 실제 소규모 기능 확인이며, 대규모 용량 통과로 확대하지 않음
- 새 수용 기록은 소스 commit과 미커밋 여부를 함께 표시. 소스 지문만으로 수정 중 빌드를 깨끗한 commit의 산출물로 표현하지 않음
- 100개 질문 중 첫 10개의 예비 평균: 전체 FTS 0.55초·결합 3.34초, 한 모음 FTS 0.36초·결합 2.55초. 전체 p95는 아직 미확정
- 전체 400회 완료: 각 100회 성공, p95 전체 FTS 0.563초·결합 3.413초, 한 모음 FTS 0.376초·결합 2.643초
- 이 성능 질문은 `자료 00000` 같은 문서 번호. FTS 회수율은 양쪽 100%, 명시적 의미 모드는 전체 4%·한 모음 32%. 의미 질문 품질 기준을 대신하지 않으며 정확한 번호에는 FTS 사용
- FTS의 벡터 실행 횟수를 100으로 잘못 명명한 집계는 raw 응답 대조 후 0으로 정정. 원본 보존, 시간·질문·회수율 불변. 의미 모드의 실제 벡터 실행은 각 100회
- 우선 보강: 한 현재 snapshot 안에서 FTS와 의미 후보 재수화에 같은 `PreparedRagIndex` 사용. 중복 전체 해시·역직렬화만 줄이며 최종 현재 디스크·권한·정본 검사는 유지
- 후속 탐색: [ONNX Runtime SessionOptions](https://onnxruntime.ai/docs/api/python/api_summary.html)의 결정적 계산과 그래프 최적화 수준 비교
- 기준 구성과 `use_deterministic_compute=true`, 그 상태의 BASIC·DISABLE_ALL을 각각 분리. 모델 파일은 유지하고 시작 시간·동일 입력 벡터·조회 시간을 함께 검사
- 설정 이름만으로 재생성 결함 해결을 주장하지 않으며, 기존 실패 벡터와 직접 비교한 실제 수용 결과 필요
- ALL/결정적 계산은 기존 512문서·500문서·30질문과 바이트 동일했으나 속도 이득 없음. BASIC·DISABLE_ALL은 출력이 달라 미채택
- 원실패 모음의 인코딩·전체 보조 실행·정확한 환경 변수 재현은 모두 새 증분/재생성 500개 동일. 두 값 모두 원래 재생성과 같고 원래 증분만 달라, 원인은 아직 미확정
- 모음별 입력 지문은 정본에서 SQLite를 만들 때 계산해 인증된 파생 테이블에 저장. 조회는 권한이 허용한 모음의 지문만 사용하며, 전체 SQLite 해시 검사는 유지
- 이전 색인에 테이블이 없으면 기존 계산 경로 사용. 새 테이블의 누락·손상은 숨기지 않으며 전체·증분 생성의 지문 동등성을 검사
- `b3013c08`·`ed588ebf` 구현. 전체 collection record의 위치 변경도 확인하고 기존 계산과 같은 지문을 반환. Wiki 148개·bundle 1개·검색 7개·Notion 8개 통과, 수동 성능 3개 별도

### 정적 임베딩 후보 비교 결과

- `VEC10-007`의 정적 임베딩 비교 대상: [potion-multilingual-128M](https://huggingface.co/minishlab/potion-multilingual-128M), MIT 표기·256차원
- 고정 revision `73908c3438cf03b6a01bcb9611d62b23d0726f08`. 가중치 512,361,560B, SHA-256 `14b5eb39cb4ce5666da8ad1f3dc6be4346e9b2d601c073302fa0a31bf7943397`
- [Model2Vec 공식 방식](https://github.com/MinishLab/model2vec): 학습된 토큰 벡터의 평균과 정규화. 원문 길이를 줄이지 않고 같은 질문·현재 FTS 점수와 비교
- 공식 속도·품질 표를 Hive의 통과 근거로 사용하지 않음. 원본 120개를 먼저 평가하고 통과 후보만 독립 60개로 확인
- 조사는 격리 파일과 기존 NumPy·tokenizers만 사용. API key·provider 호출·자동 패키지 설치·제품 의존성 변경 없음
- 원본 의미 질문 51/60, 85%로 FTS 80% 대비 5%p 향상에 그쳐 미달. 정확 질문 30/30 유지, MRR은 0.975→0.9381
- 공식 구현의 Unigram `[UNK]` 처리 분기까지 대조. 첫 진단의 임의 제외는 대체 기록으로 보존하고 공식 분기의 결과만 위 수치로 사용
- 독립 60개·고유 5만 개 후속 실행 없음. 기준 미달 후보의 규모 시험을 채택 증거로 추가하지 않음
- `vector-scale-100/static-potion/final-potion-report.json`: 동일 고정 질문·현재 FTS 점수·0.75/0.25 결합
- 같은 결합의 E5-small 후속: 56/60, 93.3%·13.3%p로 15%p 기준 미달. 정확 질문 30/30, MRR 0.975→0.9083
- E5의 문항·가중치 재조정과 독립·규모 후속 실행 없음. `static-potion/e5-small-followup/final-report.json`

### 전체 검색 시간의 측정 경계

- 과거 `probe_query.py`: FastEmbed import 뒤 모델 생성·첫 임베딩 1회가 643.45ms. cold p95가 아닌 단일 관측값
- 같은 모델·같은 질문의 임베딩 30회 p95가 37.31ms. SQLite·FTS·프로세스 생성·import·권한·정본 검사 제외
- 위 수치를 현재 전체 요청의 warm 500ms·cold 2초 통과 근거로 사용 금지. 이미 계산된 벡터의 engine 조회 시간도 별도
- 새 프로세스와 비어 있는 디스크 캐시를 구분. 파일 캐시가 준비된 반복 CLI도 매번 발생하는 초기화 비용 포함
- 실행 환경 파일 검사 진단: 같은 파일의 교차 실행에서 기존 약 0.76–0.82초, 폴더별 상대 경로 재사용 후보 약 0.50–0.54초
- 위 비교는 cProfile·다른 시험 실행 중의 원인 분석값. 모든 파일·종류·목록·SHA 검사 유지, 아직 제품 반영·전체 지연 통과 증거 아님
- 필요 시 같은 고정 ONNX 모델의 Rust 실행을 `tests/work/`에서 비교. `ort 2.0.0-rc.13`·`tokenizers 0.23.1` 후보의 버전·Cargo.lock·checksum 고정
- 기존 고정 모델·native 라이브러리만 사용. 자동 모델 다운로드·HTTP 기능·제공자 SDK 제외, 새 모델·상주 서버·제품 의존성 추가 없음
- 조사 근거: [ort의 명시적 라이브러리 경로](https://docs.rs/ort/2.0.0-rc.13/ort/environment/fn.init_from.html), [tokenizers의 파일 입력과 HTTP 기능 분리](https://docs.rs/tokenizers/0.23.1/tokenizers/)
- 비교 순서: 바이트·토큰·벡터 동등성 → 프로세스 전체 시간 → 현재 권한·정본 검증 포함 비용. 실제 실행 전까지 속도 개선·채택 주장 금지

### Windows 긴 경로와 지식 이동 수용

- 새 100개 모음에서 ONNX DLL 길이 261자로 설치 실패. 동일 파일·격리 환경에서 `site`의 `\\?\` 표기만 바꾼 import는 성공
- `0b268feb`: 검증된 DLL·모델·tokenizer native 로더 경로만 확장 표기. SQLite·정본·소유권 경로와 무결성 검사는 유지
- 같은 긴 경로에서 새 동의로 100개 설치·상태 확인 성공. 원본 5만 Wiki·5만 Raw·등록부·FTS 불변, 기존 실패 환경 보존
- 새 환경 `optimized-100/installation.json`. 생성 전 관리정보 244,800B, 새 실행 환경 375,684,712B와 이전 실패 환경 375,684,329B는 별도
- 새 측정의 고정 구성: 전체 3개 CLI×4작업, 증분 12개 CLI×1작업. 모두 합계 12, 기존 600초·30초·512MiB·1e-6 기준 유지
- Windows 실제 기능 수용: `tests/work/vector-native-i1kfz91b/receipt.json`, 바이너리 `90c78b9cc09a1a820cf03cd51375f22f592eaa63aeac5ac81289d477ead2d9ad`
- 긴 경로 8문서 설치·검색·이전 세대 복구·비활성화, 정확한 묶음 파일 목록, 새 사용자 루트의 원본 8문서 복원·벡터 재생성 통과
- 고정 소스 81개 벡터 생성·검색·비활성화, 기존 사실·FTS 보존과 소비자 `.hive` 생성 0건 확인
- 새 루트는 같은 Windows 컴퓨터 내부. 다른 물리 기기·세 운영체제·공개 패키지 수용을 증명하지 않음
- 로컬 바이너리는 커밋 전 빌드, 영수증의 수정 중 상태 보존. 최종 원격 수용은 깨끗한 소스·정확한 바이너리로 별도 실행
- 수용 도구 후속: source/consumer 설치 시간 제한 동일, source 모듈 bytecode 쓰기 방지, 모든 검사 뒤 바이너리 지문 재확인. 단위 11개·독립 검토 통과
- 첫 원격 실행 `33142270192`: 새 checkout의 `tests/work` 부재로 모델 설치 전 실패. 시험이 직접 부모 폴더를 준비하도록 수정
- 두 번째 실행 `33142416674`, 소스 `6edfe704`: Windows x64·Linux musl 실제 전체 벡터 수용 통과, macOS는 설치 자체 검사 실패
- macOS 실패의 `unknown` 분류는 아직 세부 원인 미확정. Python 배포본별 SQLite 확장 지원부터 검사하고, 미지원일 때만 CI 도구 환경에 호환 Python 준비
- 이 CI 준비는 Hive의 소비자 Python 자동 설치가 아님. [Python의 SQLite 확장 구성 조건](https://docs.python.org/3.13/using/configure.html#cmdoption-enable-loadable-sqlite-extensions)과 구분

### 개선된 100개 모음 전체 생성

- Windows 고정 `90c78b9c…`, 실제 같은 5만 청크·3개 CLI×4작업: 전체 643.5829365초, 600초 기준 미달
- 실제 vec0 벡터 50,000개·각 1,536B, 문서 50,000개·완료 cache 0개 확인. 호출 실패 0건
- 정본·등록부·FTS·이전 실패 환경 지문 불변. 실제 색인/관리정보 186,201,944B로 512MiB 기준 통과
- 위 저장량은 첫 전체 생성 완료 시점. 이후 이전 세대·재생성·증분 보존량도 별도 측정 필요
- 두 실행 환경 포함 전체 잔존 937,570,985B. 직접 파일 삭제 0건
- 관측 최대 합산 RSS 4,031,512,576B·private 7,675,129,856B, CPU 하한 5,827.0625초. 공유 RSS 중복·짧은 프로세스 누락 한계 포함
- 100회 CLI 시간 합계 1,922.273초·작업자 합계 1,652.437초. 차이 269.836초는 여러 처리 비용의 합이며 전체 wall에서 직접 차감 금지
- `optimized-100/full-summary.json`과 `full-timing-breakdown.json` 보존. 기존 후속 fresh 651.35초와 비교하되 코드·경로·동시 실행 차이를 한 수정의 인과효과로 단정하지 않음
- `3e9ba0e1`: 한 corpus의 동일 SQLite 검증/역직렬화 3→2회로 축소. 파일 읽기 2회·정본 검사 3회·승인 순서 유지, Wiki 149+1+7+8·Clippy·독립 검토 통과
- 다음 사전 고정 비교: 실제 16개 물리 코어·24개 논리 CPU·32GiB 환경에서 4개 CLI×4작업. 새 바이너리와 이전 100개 활성 세대를 고정하고 기존 품질·시간·용량·수치 기준 유지

### 혼합 CPU 수치 차이와 고정 실행

- Windows i7-13700KF: P 코어의 논리 CPU 0–15, E 코어 16–23. `EfficiencyClass` 1/0과 CPUID 종류 0x40/0x20 확인
- 같은 고정 모델·문서 16개로 초기화 CPU와 실행 CPU를 교차한 네 경우 비교. 초기화 위치와 무관하게 실행 P/E에 따라 이전 두 벡터 결과를 각각 정확히 재현
- 최대 성분 차이 0.00018461793661117554. 내부 수학 연산·명령어 선택 원인까지 확인한 증거는 아님
- 일반 4×4 전체 재생성: 532.6696798초·색인/관리정보 372,159,088B 통과, 6,500개 수치 차이로 동등성 미달
- P 코어 제한 4×4: 532.6559451초·동일 저장량, 5만 개 모두 과거 두 세대 중 하나와 정확히 일치. 두 세대 모두 일치 43,500개·첫 세대만 4,000개·둘째만 2,500개
- 위 P 코어 실험은 연구 실행기의 제한이며 제품 자동 제한 수용과 구분. 기존 실패 결과·원본 벡터·세대 감사 복사본 보존
- 제품 수정 `a4a2e46a`: 현재 보조 프로세스만 최고 성능 등급으로 제한, 기존 affinity·기본 CPU Set 교집합 준수. 부모·사용자 전역 설정 변경 없음
- 권한 안 최고 성능 코어 부재·혼합 다중 processor group은 실행 거부. 다른 운영체제의 결정성까지 보장하는 수정은 아님
- Python 실행 환경 27개·부모 8개·CLI 5개 중 39개 통과·Unix 실제 부모 종료 1개 제외. Rust 벡터 17개·Clippy 통과. 실모델 최종 수용은 별도
- 정책 근거: [Microsoft CPU Set 구조](https://learn.microsoft.com/en-us/windows/win32/api/winnt/ns-winnt-system_cpu_set_information), [프로세스 기본 CPU Set](https://learn.microsoft.com/en-us/windows/win32/api/processthreadsapi/nf-processthreadsapi-getprocessdefaultcpusets)
- 원본: `optimized-100/cpu-affinity/four-cell-summary.json`, `four-cell-final-audit.json`, `p-only/summary.json`

### 100개 모음 증분의 잠금 충돌

- 각 모음 본문 1개씩 총 100개 수정, FTS 갱신 21.0736873초 뒤 별도 벡터 측정. 원문 외 다른 자료 수정 없음
- 12개 CLI×1작업·P 코어 제한: 벡터 67.4740934초로 30초 기준 미달. 성공 55개·실패 45개
- 실패 45개 모두 정본 통합 잠금의 5초 대기 한도 초과. 19개 모음의 미게시 파일 38개도 실제 보존량에 포함
- 변경 없는 49,900개 벡터 바이트 일치 중 27,445개만 성공한 55개 모음의 실제 재사용. 실패 모음의 이전 벡터 22,455개를 재사용 성공으로 합산 금지
- 실패 45개만 4×1로 복구, 성공한 55개·본문·FTS 재실행 금지. 복구는 정합성 확인이며 최초 성능 실패 대체 불가
- 원본: `optimized-100/cpu-affinity/p-only/incremental/first-attempt-audit.json`, `failure-analysis.json`

### 같은 모델의 Rust 실행 구간 비교

- 제품과 분리된 `tests/work/vector-native-query-probe/`: `ort 2.0.0-rc.13`·`tokenizers 0.23.1`, 기존 고정 DLL·모델만 사용
- 세 질문×10회 새 프로세스의 30쌍, 운영체제 파일 캐시 초기화 없음. 수정한 동일 `ORT_ENABLE_ALL` 설정에서 모든 결과 오차 1e-6 이내
- Python p95 1.6082731초·Rust p95 0.9595525초. Rust의 파일 검사는 실제 적재 모델·DLL, Python은 전체 실행 환경으로 검사 범위 차이 명시
- Rust 최초 세부 비용: 검증 약 180ms·모델 약 203ms·토크나이저 약 366ms·계산 약 41ms. FTS·범위 권한·정본·인용 반환 비용 제외
- 제품 의존성 추가 없음. 전체 500ms 기준 충족 주장 금지. 최초 잘못된 `Level3` 설정 결과도 별도 보존
- 원본: `tests/work/vector-native-query-probe/measurements/1787895076468581800/summary.json`

### 실제 운영체제 수용 후속

- `33145811302`, 소스 `9ed2b124`: Windows x64·macOS arm64·Linux musl CLI의 실제 전체 벡터 수용 통과
- macOS의 기본 Python SQLite 확장 부재는 CI 전용 호환 Python 선택으로 해결. 소비자 Python 자동 설치 없음
- 직전 `33143621297`의 macOS 소스 첫 생성 실패는 다음 실행 성공만으로 원인 해결 처리 금지. 오류 종류 분류 보강 뒤 강제 분할·재개 재검증 대상
- 이 결과는 CPU 후속 제품 수정·공개 test.5 설치·성능·기밀 수용의 대체 근거 아님

### 초기 보조 실행기 기준선

- Windows x64, i7-13700KF, 메모리 약 32GiB, 고유 본문 50,000개, 8개 프로세스, 60초 단위 재개
- 실제 보조 실행기 전체 생성 614.38초. 600초 기준 미달. CLI의 정본 검사와 100개 물리 모음 운영 비용은 포함하지 않아 그 범위의 통과 증거 아님
- 완성 DB 202,776,576 bytes, 중간 복사본 포함 918,032,384 bytes. 후자가 512MiB 기준을 초과
- 100개 변경: 12.87초, 새 변환 정확히 100개. 변경 없는 재실행 1.21초·새 변환 0개
- 이 결과는 중복 캐시 제거·자동 정리·공유 실행 세션 보강 전 기준선. 개선 뒤 재측정 필요
- 원본 실행 자료: `final-vector-pipeline-50000.json`, `final-vector-incremental-100.json`

### 독립 질문 결과

- 모델 순위·기존 질문을 보지 않은 별도 작성자의 60문항: 표현 변형 40개, 교차 언어 20개
- 고정 RRF 60 비교: FTS 46.7%, MiniLM 결합 96.7%, E5 small 결합 91.7%
- MiniLM의 독립 질문 향상폭 50%p. 기존 120개 시험의 향상폭 미달을 지우는 근거로 사용 금지
- 문항·정답·상수 변경 없이 평가. 기존 정답표의 별도 근거 감사 결과는 아래 한계 절에 기록

### 새 조합의 질문·순위 재검증

- 고정 `2707…06ac9`·계약 `76aab2d3…f707fc`, 합성 Wiki 30개·원본 120문항·독립 60문항. 원본·정답표·가중치 변경 없음
- 원본 의미 질문 60개: FTS 정답 포함률 80% → 결합 96.7%. 독립 의미 질문 60개: 46.7% → 96.7%
- 각 질문마다 새 CLI·모델을 시작한 p95는 원본 의미 1.779940초·독립 1.799035초. 운영체제 파일 캐시 비움 없음, 5만 검색·모델 유지 방식의 warm 500ms 근거 아님
- 정확 질문 30개는 두 경로 모두 정답 포함률 100%. 다만 강제 `--mode semantic`의 정답 순위는 6개에서 하락: 평균 역순위 0.975 → 0.871944
- 해당 하락은 기존 `39842b7a` 점수 결합 결과와 동일. 새 병렬 처리 회귀와 구분하며, 정답 포함률만으로 모든 순위 무회귀 주장 금지
- 6개 중 같은 버전 문자열 `0.10.0` 질문 2개는 서로 다른 문서를 정답으로 지정. 기존 정답표의 모호성 한계이며 문항 임의 제거·재채점 없음
- 기본 사실 검색은 기존 FTS, 명시적 의미 검색만 결합이라는 현재 제품 계약 유지. 강제 의미 모드의 순위 손실과 기본 경로 보존은 별도 판정
- 원본: `tests/work/scope-audit-20260828/parallel-vector-queries-original-isolated.json`, `parallel-vector-queries-holdout-isolated.json`, 이전 `score-vector-queries-original-isolated.json`

#### 순위 보호 뒤의 재검증

- 제품 `64ef6082`, 바이너리 SHA `e863be946792e3321c5fd012de90c08e0a1056b962ae09944580ccee2a487aae`. 보조 실행 계약 `76aab2d3…f707fc`·모델·원본 질문·가중치 불변
- Windows 원본 120개·독립 60개 실제 조회 완료. 의미 정답 포함률 96.7% 양쪽 유지, 정확 30개 정답 포함률 100%·평균 역순위 0.975·질문별 순위 손실 0개
- 순서 보호 관측: 원본 26개, 독립 질문 0개. 이전 6개 손실을 원본 수정이나 기본 FTS 평가로 대체하지 않고 강제 의미 모드에서 재검증
- 30문서 환경 새 CLI p95: 원본 의미 1.961756초·독립 1.810315초. 파일 캐시 비움 없음, 대규모·모델 유지 warm 기준과 구분
- 경계·원문 예산·중복·인용·응답 형식 반례와 독립 검토 통과. 해당 crate 검사: CLI 441개·Wiki 179개 통과, 수동 4개 제외. 형식 검사 13개·Python CLI 6개 통과, Clippy 자체 경고 0개
- `74ede391`: 채택 판정에 고정 30개 질문의 완전한 `exact_rank_comparisons` 추가. 누락·중복·부분집합·잘못된 순위와 평균값으로 가린 개별 하락 차단, 모의 5개 검사 통과
- 과거 근거 파일 변경 없음. 보강된 검사에서 순위 자료 부재는 미통과이며 정답 포함률만으로 채택 금지
- 원본: `literal-vector-queries-original-isolated.json`, `literal-vector-queries-holdout-isolated.json`, `vector-literal-gold.log` — 모두 `tests/work/scope-audit-20260828/` 아래

### 저장량 보강 실행 결과

- `385bc131`: 이전 제어 기록에 있는 오래된 복사본만 정리, 삭제 실패는 지속 목록으로 재시도
- `ff9244ec`: 완성본의 중복 벡터 제거, 512행 복원 cursor, 256쪽 단위 증분 공간 회수
- 기존에 측정한 고유 벡터 5만 개로 실제 sqlite-vec 실행. 완성본 91,705,344 bytes, 변경 뒤 91,754,496 bytes
- 최초 최종 조립·공간 회수 10.00초, 무변경 재실행 0.84초·DB 해시 동일
- 1초 작업 한도에서 복원 도중 중단, 이어서 100개만 새 변환. 나머지 49,900개 벡터 바이트 일치
- 실제 Windows CLI 30개 문서: 파일 잠금으로 삭제 실패 유도, 다음 생성에서 재시도, 현재·이전 정상 세대 두 개 유지
- 정본·등록부·FTS 바이트 변화 0건. 다른 파일 보존. 전용 폐기 가능 cache 밖의 삭제 없음
- 이 저장 시험은 새 모델 변환 5만 개·100개 collection·최종 전체 경로의 속도/최대 용량 증거가 아님
- 원본 실행 자료: `vector-compaction-50000.json`, `vector-retention-cli.json`

E5: 검색어와 문서의 역할을 구분해 학습한 비생성형 임베딩 모델.
공식 사용법의 `query: `·`passage: ` 접두부와 평균 집계·정규화 적용.
출처: [모델 카드](https://huggingface.co/intfloat/multilingual-e5-small), [공식 연구 저장소](https://github.com/microsoft/unilm/tree/master/e5).

| 후보 | 고정 revision | 모델 파일 | 상태 |
| --- | --- | --- | --- |
| E5 small | `614241f622f53c4eeff9890bdc4f31cfecc418b3` | `onnx/model_qint8_avx512_vnni.onnx`, 118,346,824 bytes | 이번 품질 비교 미달 |
| E5 base | `d128750597153bb5987e10b1c3493a34e5a4502a` | 같은 이름, 278,686,411 bytes | 이번 품질 비교 미달 |

- 두 모델의 공식 표기 license: MIT. [E5 base 원본](https://huggingface.co/intfloat/multilingual-e5-base)
- 모델·토크나이저는 고정 revision의 LFS SHA-256 또는 Git blob 식별자와 대조 후 사용
- 제공자 API·API key·생성형 모델 실행 없음. 기존 격리 환경의 로컬 ONNX 계산만 사용
- E5 small의 문서·질문 150개 변환: 약 2.49초. 고유 5만 청크 전체 생성 시간으로 환산하거나 통과 처리 금지

## 다음 검증

### 공유 모음 사전 검사 병렬화 후보

- 버퍼 비교의 첫 16모음: 기존 5.374초 → 개선 5.073초 → 기존 5.317초. 세 번 모두 완료·새 변환 0개, 전체 갱신 기준의 대체 근거 아님
- 다음 후보: 공유 모음 입력 준비 뒤 기존 `check_shared_partitions`로 최대 4개 모음의 정본 검사 동시 수행
- 기존 `shared_semantic_corpora`와 작업 수 1은 종전 검사 순서 유지. 새 제한형 API만 현재 공유 묶음 CLI에서 사용
- SQLite·승인 지문·본문 추출·청크/바이트 예산은 주 스레드 유지. 정본·등록부·권한 검사 생략과 새 파일 읽기 구현 없음
- 모든 정본 검사 성공 후에만 본문 반환. 비공개·기밀·소스·FTS 경로 불변, 게시 시 기존 4개 병렬 검사 유지
- 새 병렬 경로의 차이: 본문 준비·예산 오류를 정본 검사보다 먼저 반환 가능. 기존 직렬 API와 오류 우선순위까지 동일하다는 주장 금지
- 반례: 직렬/병렬 입력·지문 일치, 정본 손상·권한 변경의 본문 반환 거부, 잘못된 예산, 스레드 시작 실패·panic의 전체 합류
- 검증 순서: 관련 Rust·Python·독립 검토 → 고정 바이너리 비교 → 실제 100개 증분·같은 입력 전체 재생성. 모델·실행 계약·허용 시간 불변
- 구현 `4e971a78`: 관련 Rust 626개 통과·수동 4개 제외, Python 19개·Clippy·독립 검토 통과. 새 성능 수용은 별도 미완료
- 후속 실행: 현재 입력 5만 개 fresh 재계산 후 새 100개 변경 실험과 전체 검색. 원본 질문·모델·실행 계약 유지, 기존 실패 기록 보존
- 제어 기준: 버퍼 비교 뒤 `current-control-baseline.json` SHA `72c248c8…ad309a0`, 독립 벡터 참조 `89dd01cb…8f5930`. 오래된 활성 DB 경로 재사용 금지
- 전체 생성은 60초·16작업·최대 12회, 미완료 모음만 재개. 진전 없는 재시도 금지. 증분은 모음별 정확히 1개 변경·1회 FTS 갱신, 별도 최초 변경 시각 기록
- 같은 실행 계약의 fresh 검증은 미시작 모음의 `fresh`와 이번 생성의 검증된 중간 결과 `resume`을 분리. 기존 active 재사용을 새 5만 개 계산으로 집계 금지
- 새 검색 시간은 전역/한 모음·FTS/의미 모드별 집계. 숫자 질문의 지연 검사이며 표현이 다른 질문의 품질 평가와 구분

### 제한된 파일 읽기 버퍼 후보

- 현재 Rust 1.97 표준 구현과 cap-std 4.0.2 소스 확인: `Take`의 일반 `read_to_end` 경로는 빈 버퍼에서 작은 탐색 읽기로 시작
- Hive의 `read_named_file`은 열린 파일의 종류·크기를 먼저 검사하지만, 그 길이를 버퍼 힌트에 사용하지 않는 상태
- 후보: 검증된 길이와 1바이트 여유만 사전 확보. `usize` 변환·덧셈·할당 실패는 오류로 반환하고 실제 읽기는 기존 `Take(max+1)` 유지
- 길이는 힌트일 뿐. 파일 축소·증가 때도 실제 EOF·최종 초과 검사를 유지하며 `read_exact`·미리 채운 버퍼로 대체 금지
- 디렉터리 고정·nofollow·파일 정보·비밀 값 검사·본문 파싱·지문 검증 변경 없음. 새 의존성·unsafe·전역 캐시 없음
- 대표 작은 파일의 호출 수, 짧은 읽기·크기 증감·상한·입출력 오류·바이트 동일성 단위 검사 후 실제 경로로 재확인
- 메모리 부족이나 파일 축소 상황에서는 종전보다 일찍 할당 오류 가능. 숨기거나 읽기 검사 완화 금지
- 이 후보는 전체 59초 지연의 원인 확정이 아님. A/B/A 자료와 별도로 평가하며, 개선 전후 성능 기준 유지

#### 구현과 비교 준비

- `5e43bb00`: 크기 힌트 구현, 독립 검토 통과. Windows 관련 Rust 623개 통과·수동 4개 제외, Python 19개 통과, Wiki Clippy 통과
- 대표 1,000바이트 입력의 `Read` 호출 7회 → 2회, 결과 바이트 동일. 실제 5만 자료의 속도 개선과 구분
- 첫 Rust 실행은 병행 Python 시험의 `hive.exe` 점유로 시작 실패. 실행 순서 분리 후 통과, 실패 로그 보존
- 비교 전 기존 A/B/A: `2707` 35.279초 → `b190` 34.280초 → `2707` 33.292초. 100개 모음 모두 완료·새 변환 0개, 5만 벡터 바이트 보존
- 중간 `b190`의 구간 합계: 정본 읽기 9.766초·준비 4.278초·실행기 3.484초·게시 14.262초. 앞선 정본 읽기 59.692초 현상 미재현, 캐시·호스트 영향의 원인 분리 증거 아님
- A/B/A는 정상 제어 세대 변경을 포함한 무변경 진단. 증분 100개 56.313초의 30초 미달 판정 유지
- 후속 버퍼 비교는 A2의 현재 제어 상태를 기준으로 이전→개선→이전 순서 고정. 과거 독립 벡터 복사본은 비교 가능하지만 오래된 제어 경로 재사용 금지
- 원본: `vector-read-buffer-crate-tests-retry.log`, `vector-read-buffer-tests.log`, `vector-scale-100/optimized-100/shared-list-next/wave4-next/aba-unchanged/results/comparison-observations.json` — 모두 `tests/work/scope-audit-20260828/` 아래

### 정확한 조회의 순위 보호

- 기존 판정의 정확 질문 Recall 검사만으로는 순위 무회귀 증명 불가. 기본 FTS 보존만으로 채택 기준을 사후 축소하지 않고, 질문별 역순위 비교 추가
- 보호 후보: 기존 FTS의 정확한 별칭 일치, 경계가 일치하는 단일 식별자/버전/날짜/숫자, 명시적 인용 문구, 정본 안의 연속된 세 단어 이상과 일치하는 전체 질문
- 일반 단일 개념어나 두 단어 조합이라는 이유만으로 보호 선택 금지. 구조화된 문자열의 접두사 충돌도 제외
- 보호 시 특정 문서만 앞으로 옮기지 않고 FTS 후보 전체의 기존 순서를 유지한 뒤 의미 검색 전용 후보 추가. 가중 점수의 인위적 증가 없음
- 같은 문구의 반박·인용도 사실 확정 근거가 아님. 정본 인용·권한·바이트 예산 검사 유지, 중복/충돌 후보는 기존처럼 거부
- 점수 계산과 최종 순위 정책을 구분하고 실제 FTS 순서 보호 여부를 조회 결과에 표시. source·사용자 지식에서 같은 판정 규칙 사용
- 먼저 규칙 고정·일반 반례 검사. 이후 원본 120개·독립 60개를 그대로 재검증하고, 정확 질문은 각 정답 순위·의미 질문은 기존 향상폭 기준 적용
- 별칭 전용 일치·버전 접두사·일반 개념·부정/인용·예산·비공개 격리·중복·FTS 선두가 비문구인 경우 포함. 골드 ID별 예외·문항 제외·가중치 재조정 없음
- 모델·보조 실행 계약·색인 형식·기본 FTS 변경 없음. warm 500ms와 대규모 조회 시간은 별도 미완료 조건 유지

### 증분 처리 단계별 시간 확인

- 새 병렬 조합의 100개 증분 56.312915초로 30초 미달. 변경 없는 벡터 49,900개·정본·실패 자료 보존과 442,763,989B 용량은 통과
- FTS 갱신 1회 19.919초는 별도. 첫 본문 수정부터 전체 처리 77.519837초, 원본 100개 변경은 단 한 번 적용
- 중첩된 모음별 시간을 빼서 병목 추정 금지. 공유 목록 CLI의 실제 단계별 벽시간을 먼저 측정
- 명시적 공유 목록 결과에 준비·입력 확인·중간 파일 준비·보조 실행·검증/게시 구간의 시간만 추가. 실패한 구간도 관측한 범위 표시
- 새 명령·상시 기록·모델/색인 형식·승인·정본 검사 변경 없음. 모음 이름·본문·질문·비밀 값 추가 출력 없음
- 시간 구간의 오류 보존 단위 검사 뒤 같은 입력의 무변경 진단으로 기존 처리 비용 확인. 무변경 시간을 100개 증분 통과 근거로 사용 금지
- 이후에는 실제 큰 비용이 확인된 구간만 개선. 최신 증분 참조와 기존 실패·성능 기록 보존, 기존 30초/600초/512MiB 기준 유지

### 순차 모음 계산의 후속 성능 후보

- 고정 목록 전체 생성 932.943초, 증분 100개 95.943초로 시간 기준 미달. 5만 벡터 동등성과 누적 442.8MB 보존은 통과
- 실제 모음별 실행 시간 합계는 전체 860.316초·증분 51.831초. 나머지 72.627초·44.112초를 전부 정본 검사 비용으로 단정하지 않음
- 후속 후보: 64개 입력 묶음은 유지하고 독립 DB를 최대 4개씩 같은 모델로 동시 처리
- 동시 DB 수와 DB별 변환 작업 수의 곱은 요청한 전체 작업 수 이하. 추가 DB 진행 스레드·메모리는 별도 측정
- 모델 초기화 전체를 잠금으로 보호하고 성공 뒤에만 공유. 초기화 실패는 해당 요청 전체의 실패이며 다른 스레드에서 재초기화 금지
- SQLite 연결은 각 DB 담당 스레드가 생성·종료. 시작한 묶음은 실패가 있어도 모두 기다리고 입력 순서로 처리. 전체 기한 뒤 새 묶음 시작 금지
- 공통 최종 조립 기한·부모 강제 종료 상한·실패한 구간의 게시 금지 유지. 원본·독립 질문·벡터 오차·시간·용량 기준 변경 없음
- 기존 4개 CLI×4작업의 속도 통과는 설계 참고이며 새 구성의 통과 증거 아님. 새 실행기 동의 후 16작업 후보의 모의/실제 동등성 검사와 재측정
- 별도 후보: Rust 게시 전·후 정본 검사만 최대 4개 병렬화. 기존 API·1작업은 직렬 유지, SQLite 연결은 주 스레드 전용
- 기존 권한·정본 검사 순서와 전체 파일 검사를 유지. 스레드 생성·실행 실패도 모두 합류 후 오류로 바꾸고 기존 역순 복구 경로 적용
- 준비 단계의 항목별 오류 우선순위는 변경하지 않음. 최종 등록부·현재 SQLite 전체 해시 검사 유지. 두 성능 변경은 별도 검증·커밋

#### 구현과 근거

- `b52d8550`: 보조 실행기 DB 묶음 병렬화. Windows 모의 33개·관련 49개 중 48개 통과, Unix 부모 종료 1개 제외. CLI 전체 437개 통과·수동 1개 제외
- `ddb91859`: 게시 전·후 정본 검사 병렬화. Wiki 176개 통과·수동 3개 제외, CLI 벡터 20개 통과·수동 1개 제외, Clippy 통과
- 두 변경의 독립 검토 통과. 신규 모델 초기화·DB 연결 소유권·전체 작업 수·실패 합류·오류 순서·역순 복구 검사 포함
- 단위/계약 검사는 새 조합의 실제 수치·메모리·성능 통과 근거가 아님. 기존 원본 질문·독립 질문·현재 5만 벡터 기준 유지
- 순차 전체 자료: `optimized-100/shared-list-next/full100-measurement/summary.json`, 증분: `incremental100/vector-after-refresh/summary.json`
- 증분 준비 오류 보존: 시험 도구의 SQLite 연결 미종료로 FTS 잠금 실패, 이어서 내용 기반 ID 변경을 반영하지 못한 대조 오류. 제품 오류와 구분
- 연결 종료·ID 대조 수정 뒤 본문 재수정 없이 복구. 성공한 FTS 18.742초와 벡터 95.943초 별도, 실패 FTS 포함 133.568초
- 첫 변경부터 진단·복구 대기 포함 약 540.003–540.005초. 첫 절대 시각 부재에 따른 범위 값이며 정밀 단일 측정값으로 보고 금지
- 최종 조합 `ddb91859`·문서 `6e7c4a3c`: Windows 전체 Rust 877개 통과·수동 4개 제외. Python 단일 전체 실행 783개 중 743개 통과·OS/호스트 조건 40개 제외, 누락·실패 0개
- Python 구간 시간: 문서 1.968초·보안 67.813초·계약 442.500초·통합 252.062초·출시 45.407초. 같은 시간에 기능 수용도 실행해 성능 비교 자료로 사용 금지
- 원본: `tests/work/scope-audit-20260828/vector-parallel-workspace-tests.log`, `vector-parallel-python-all.log`, `vector-parallel-all-timing.json`, `vector-parallel-reconciled-tests.json`
- 새 바이너리 SHA `2707deaea7c2dc215f99dee8ec56a6bb9ef86e58a6adfed2d9a2d160b3106ac9`: Windows 실제 전체 61호출·기밀 승인/공유 격리·손상 뒤 FTS 복귀 통과. `tests/work/vector-native-lblw2nrc/receipt.json`, `tests/work/vector-privacy-current-cqikgvng/receipt.json`
- 위 실제 시험은 합성 자료·해당 Windows 호스트 범위. 5만 성능·다른 운영체제·실제 사용자 기밀 지식 검증 대체 금지

#### 병렬 전체 생성 최종 결과

- Windows 기준 호스트·고정 바이너리 `2707…06ac9`, 새 계약 `76aab2d3…f707fc`, 명시적 16작업·60초 구간·9호출
- 최초 상태 확인부터 모든 재개·기록 포함 577.809868초로 600초 통과. 목록 실행만 577.076580초, 사전 파일 감사 33.989초·사후 읽기 감사 24.524초 별도
- 100개 모음·새 변환 50,000개·실행 실패 0개. 이전 계약 캐시 재사용 없음
- 최신 독립 참조와 실제 벡터 50,000개 모두 바이트 일치, 최대 수치 오차 0. DB 파일 자체는 계약/관리 정보 차이로 별도 판정
- 정본·등록부·FTS·실패 38파일·옛 실행 환경/근거 보존, 수동 정리 0개
- 색인·중간·제어·격리 자료 합계 442,768,085B로 512MiB 통과. 누적 네 세대 실행 환경 1,502,753,490B 별도
- 0.5초 표본의 실행기와 자손 합산 최고 RSS 4,612,571,136B·전용 메모리 5,549,211,648B. RSS의 공유 페이지 중복·짧은 프로세스 누락 가능
- 모음별 실행 시간은 서로 겹치는 벽시간. 합계를 전체 벽시간에서 뺀 나머지를 상위 처리 비용으로 해석 금지
- 측정 중 별도 모델·컴파일·전체 검사 없음. 소규모 문서/Git/원격 CI 확인과 바이너리 SHA 1회 등의 경량 관리 활동은 별도 기록
- 원본: `tests/work/scope-audit-20260828/vector-scale-100/optimized-100/shared-list-next/wave4-next/full100-measurement/summary.json`
- 새 조합의 증분 100개·변경 뒤 재생성·전체 검색 속도 통과 근거는 아직 별도 필요
- 원격 `33160056855`, 깨끗한 소스 `41db2ddf`: 새 병렬 조합의 실제 벡터 수용 세 운영체제 모두 통과. Windows 61호출, macOS·Linux 각 60호출

### Windows 수용 관측 방식 보강

- 원격 `33153601054`: macOS arm64·Linux musl 수용 통과. Windows는 모델 취소 관측용 PowerShell/CIM 조회의 10초 시간 초과로 미완료
- 제품의 종료 실패와 구분. 원격 Windows의 부모 강제 종료는 이 실행에서 검증하지 못한 범위
- 관측을 Toolhelp 목록·승인 Python 이미지·RSS·두 번째 부모 목록·생존 핸들로 대체. 새로 생긴 해당 scope staging도 확인
- 후보 핸들은 모든 검사 전 종료 권한이 아님. 실패 시 닫기만 하고, 검증 완료 뒤 같은 핸들로만 종료 확인·정리
- PowerShell·WMI 서비스 준비 시간에 의존하지 않는 운영체제 직접 조회. 명령행 helper SHA나 추론 명령 실행 시점을 직접 관측했다는 주장 금지
- 반례 검사 뒤 실제 Windows 수용 재실행. 제품 바이너리·모델·성능 측정 자료 변경 없음
- `ddbd5908` 도구의 로컬 Windows 전체 61호출 통과: `tests/work/vector-native-f6f5t3n2/receipt.json`, 실제 제품 바이너리 SHA `5fac9c18bd87b731a7afa2a09634af9e7c07d81fcfec7bd4dda80bbe10687793`
- 부모 37608 종료 뒤 관측 자식 25432 종료·출력 닫힘·활성 색인 보존·새 변환 0개 복구. 새 병렬 처리 제품의 수용과 구분
- 원격 `33158174868`, 소스 `ddbd5908`: Windows x64·macOS arm64·Linux musl의 실제 벡터 설치·검색·이동·소스 수용 모두 통과. 병렬 처리 후속 제품·공개 test.5 수용은 별도

### Windows 명령 강제 종료의 자식 잔존 수정 계획

- 실제 `c051` 모델 시험: 부모 Hive만 종료한 뒤 출력 파이프가 10초간 열림, 이미 확보한 자식 핸들로 별도 정리 필요
- 실패 자료: `tests/work/vector-cancel-current-657d14iq/receipt.json`. 정본 영향 없음, 강제 종료 수용 실패로 유지
- 원인: 고정 `process-wrap 9.1.0` 동기 API의 `make_job_object(handle, false)`. 명시적 트리 종료는 지원하지만 부모 사망에 따른 Job 종료 옵션은 비활성
- [공식 라이브러리](https://github.com/watchexec/process-wrap)의 `KillOnDrop`은 Tokio 전용. 기존 동기 API 유지와 새 감시 방식 추가 대신 같은 라이브러리의 한 줄 보정 선택
- 원본 `.crate` SHA-256 `2e842efad9119158434d193c6682e2ebee4b44d6ad801d7b349623b3f57cdf55`, Apache-2.0/MIT·저작권·전체 패키지 파일 보존
- `vendor/process-wrap-9.1.0/`에 원본을 보관하고 Windows 동기 Job 생성의 두 번째 인자만 `true`로 변경. Cargo 캐시·전역 설치 수정 금지
- Cargo 경로 패치·workspace 제외, 원본 파일별 지문과 허용된 한 줄 차이의 자동 검사. upstream이 동기 옵션을 제공하면 재검증 후 로컬 패치 제거
- 검증: 기존 버전에서 부모만 종료한 반례 재현 → 한 줄 수정 → 실제 모델 종료·이전 정상 세대 복구 → 관련 Git/모델 프로세스·세 운영체제 검사
- 종료 보장 범위: Job 할당 뒤 실행한 자식 트리. 생성과 Job 할당 사이의 짧은 중단 구간까지 원자적 보장으로 표현 금지
- `4fdf6c27`: 같은 반례의 수정 전 실패·후 통과, 원본 84개 파일·압축 패키지 지문 검사·라이선스 보존. [외부 소스 안내](../../vendor/README.md)
- 실제 모델 `5fac9c18bd87b731a7afa2a09634af9e7c07d81fcfec7bd4dda80bbe10687793`: 관측 자식 메모리 418,926,592B, 부모만 종료한 뒤 자식·출력 파이프 종료 확인
- 활성 제어 기록·정본·FTS 불변, 새 실행의 변환 0개·기존 정상 DB 지문 재사용. `tests/work/vector-cancel-current-4lbmwxvy/receipt.json`
- 원본 동기 API의 비활성 선택 기능에서 unused 경고 6건 보존. 자체 코드 Clippy 오류 없음, 경고를 없애기 위한 추가 외부 소스 수정 없음
- 실제 수용 도구도 보강: 후보 핸들 검증 뒤에만 종료 권한 부여, 생성 시각으로 PID 재사용 거부, 종료 직전 생존 확인, 예외 때 정리·기록 보장
- 검토 중 중단한 `vector-native-qi9p03le`은 부분 결과만 보존하고 전체 수용에서 제외. 새 도구 단위 20개·독립 검토 통과, 새 전체 실행 진행 중

### 최신 전체 검사와 기밀 회귀

- Windows 전체 Rust: 874개 통과·수동/대규모 4개 제외, workspace Clippy 통과. 원본 외부 라이브러리 경고는 위에 별도 명시
- Python 전체 다섯 구간을 한 번 실행해 통과, 이후 출처/취소 도구 보강 시험과 합산한 현재 목록 778개: 738 통과·40 OS 조건부 제외·실패/누락 0개
- 합산 결과를 Job 보강 뒤 단일 `--all` 실행으로 표현 금지. 전체 로그 `vector-final-python-all.log`, 후속 목록 대조 `vector-reconciled-tests.json`
- `c051`의 새 격리 기밀 검사: 생성/조회 승인 분리·일회성 소비, 공개 검색의 비공개/기밀 배제, 명시적 비공개 조회, 보조 코드 손상의 FTS 복귀 통과
- `tests/work/vector-privacy-current-4g28310y/receipt.json`: 정본·등록부·FTS 보존, 실제 사용자 기밀은 사용하지 않은 Windows 시험
- 앞선 기밀 검사 도구의 120초 만료·승인과 조회의 서로 다른 top-k는 계약에 맞지 않아 거부. 도구를 60초·같은 요청 예산으로 수정, 제품 승인 경계 완화 없음

### 묶음 갱신과 검색 재사용 구현 결과

- `f463f73f`: 공유 모음의 입력/게시 검증 한 번씩 공유, 모음별 정본·권한 검사와 역순 제어 기록 복구
- `bd54512a`·`a3a09ff6`: 모델 한 번 공유, 명시적 목록과 전체 시간 예산, 부분 완료·미시작 상태 구분
- `124569e0`: 같은 질문의 초기 RAG 연결을 최종 결합에 재사용. 새 전체 파일 해시·manifest/등록부·반환 정본 검사는 유지
- Wiki 157개·묶음 1개·검색 7개·Notion 8개 통과, 별도 대규모 3개 제외. CLI 벡터 21개 통과·수동 시험 1개 제외, Clippy 통과
- `569666f2`: `knowledge-maintain` 현재 배포본 네 곳과 목록 지문 갱신. 과거 파일 불변, 직접 test.2/test.4 갱신을 포함한 Python 35개 통과·OS 전용 2개 제외
- Windows 실제 18개 모음·76개 문서, 바이너리 `ee07d7444f12a9589c146421a947c593e27c1d758ffa6f96935462922a7deffe`: 두 구간 전체 생성·무변경 76개·증분 2개와 나머지 74개 벡터 바이트 일치
- 단일 생성과 묶음의 세 모음 12개 벡터 일치. 증분 뒤 단일 재생성 9개 벡터·문서 지문 일치
- 증분/전체 DB 파일 SHA 차이는 남은 `restore_cursor`와 SQLite 내부 `schema_version` 차이. 표 정의·문서·벡터는 동일, 물리 파일 전체의 byte 동일 주장 제외
- 1초 예산: 완료 1·준비만 완료 15·미시작 2. 미완료 17개만 재개해 완료, 실제 embedding checkpoint 관측은 없음
- 중복 별칭·비공개/기밀 공개 범위·마지막 모음 비활성화 거부에서 전체 영구 파일·경로 보존. 전용 기밀 내용 누출 시험의 대체 근거 아님
- 위 로컬 바이너리는 커밋 전 CLI 연결본이며 출처 기록에 수정 중 상태 명시. 겹친 컴파일 6.3초를 별도 정정, 시간값은 진단용으로만 사용
- 원본: `vector-scale-100/batch-cli-acceptance/acceptance.json`, `physical-difference-audit.json`, `timing-context-correction.json`
- `c264d0e3`: 세 운영체제 수용 도구에 별도 3모음·24문서 갱신, 소스 1초 분할과 두 차례 전체 재생성 추가. 단위 15개·독립 검토 통과, 새 실제 수용 진행 중

### 단일 검색 안의 SQLite 적재 재사용 계획

- 초기 검색 범위 확인과 최종 FTS·벡터 결합이 같은 RAG를 다시 해석하는 비용 축소
- 같은 `RagStore`·전체 요청에 결합한 비공개 일회성 검색 상태만 유지. 다른 요청·모음·사용자 루트 재사용과 복제 금지
- 모델 실행 중 정본 잠금 해제. 최종 단계에서 dirty·manifest/trust·전체 등록부·현재 SQLite 바이트를 새로 확인
- 전체 파일 SHA·manifest 전체·등록부 전체가 초기 값과 같을 때만 기존 읽기 전용 연결 재사용. 논리 지문 하나만의 대조 금지
- 최종 후보·요청 예산·반환 정본·scope 제어 기록 검사 유지. 실패 시 상태 폐기 후 기존 FTS를 새로 검증
- 기밀 승인 소비 순서 유지. 새 공개 명령·서버·다음 질문까지의 캐시 없음
- 검증: 중간 SQLite/manifest/trust/등록부 변조, 반환 원문 변경, 다른 요청 사용 불가, FTS 대체와 메모리 비용
- 요청 사이 모델 재사용은 별도 선택지이며 이 변경에 포함하지 않음

### 증분 처리의 반복 비용 축소 계획

- 목적: 100개 모음의 반복 모델 시작·전체 RAG 적재 감소. 30초 기준 유지, 기존 실패 기록 보존
- 기존 `knowledge vector rebuild`의 `--collections` JSON 배열로 정확한 목록 지정. 단일 `--collection`과 혼용 금지, 자동 전체 모음 선택 없음
- 같은 사용자 루트·동일한 승인 실행 환경·이미 활성화된 shared 모음 최대 100개만 허용. 별칭 중복·미승인·source·private·confidential 묶음 거부
- 한 실행은 최대 16개 모음씩 처리. 각 구간의 전체 입력은 기존 5만 청크·256MiB 한도 준수, 전체 실행 예산 만료 시 나머지는 미시작으로 반환
- 구간별 RAG 적재·검증 한 번과 모델 한 번만 공유. 모음별 DB·제어 기록·불변 세대·정본 지문·물리 경계 유지
- 정렬된 scope 잠금 → 정본 잠금 안 입력 검사 → 정본 잠금 해제 후 계산 → 새 RAG/정본/권한 검사 뒤 게시
- 게시 구간의 중간 변경은 게시한 제어 기록만 역순 비교 후 복구. 구간 간 전체 원자성 주장 없음, 완료·미완료·미시작 구분
- 새 저장 형식·상주 서버·자동 설치·권한 확대 없음. 단일 모음 경로와 공통 계산/게시 절차 재사용
- 구현 순서: 공유 입력/게시 API·회귀 → 제한된 보조 계산 → CLI 연결 → 실제 단일/묶음 동등성·잠금·증분 측정
- 필요한 반례: 중복/범위 거부, 다른 실행 환경, 도중 정본·권한·SQLite 변경, timeout/취소, 다른 모음 보존, 기존 단일 명령과 경합
- 전체 검색의 500ms/2초 기준은 별개. 증분 최적화로 검색 지연 해결 주장 금지

### 대규모 실제 자료에서 발견한 저장 한도 불일치

- 실제 Wiki 50,000개·shared collection 100개로 만든 RAG SQLite는 197,865,472 bytes
- RAG 허용 한도 256MiB와 공통 파일 게시기의 128MiB 한도가 달라 정상 자료의 게시 실패
- 자료나 검증을 줄이지 않고 게시기 한도를 RAG 계약과 일치시킴. 한도 초과 거부는 유지
- 실패한 임시 파일 검증에서도 생성한 임시 파일을 정리하고 기존 파일을 보존하도록 회귀 추가
- 실패 때 원본 문서·등록부는 보존됨. 이 실패는 벡터 모델 실행 전의 기존 저장 경로 결함
- 수정 바이너리 `5207bf9109e43f9918e3f01777dc5c4952fce612a4119447fb266f8e52a9d19a`: 실제 FTS 갱신 17.929초, 문서·청크 각 5만 개 정상 저장
- 첫 FTS 갱신에서 등록부의 JSON 표현이 YAML로 정규화됨. 101개 항목의 모든 논리 내용과 정본 문서는 동일, 등록부 바이트 동일 주장은 하지 않음
- 이후 벡터 설치·생성의 등록부 보존 기준은 이 정상 FTS 준비 직후 바이트로 고정
- `vector-scale-100/preparation.json`, `registry-normalization.json`, `cleanup-receipt.json`: 실패 임시본은 활성 색인과 전체 해시 일치 확인 뒤 정확한 파일 하나만 정리

### 추가 승인 후보: 비생성형 로컬 재정렬

- 의미 후보 검색 뒤 질문·문서 쌍을 함께 읽고 관련성 점수만 계산하는 별도 모델
- [Sentence Transformers의 공식 검색·재정렬 방식](https://www.sbert.net/examples/sentence_transformer/applications/retrieve_rerank/README.html)은 빠른 후보 탐색과 정밀 순위 계산을 분리
- 공개 후보 [다국어 mMiniLMv2 재정렬 모델](https://huggingface.co/cross-encoder/mmarco-mMiniLMv2-L12-H384-v1): Apache-2.0, ONNX 제공. 한국어 정확도·현재 CPU 지연은 미검증
- 기존 로컬 실행 예외는 임베딩 색인 계산에 한정. 재정렬 모델 실행·의존성 추가 전 유지보수자 승인 필요
- 승인 시 원본·독립 질문을 변경하지 않고, 정확한 사실 검색 무회귀·전체 지연·모델 용량을 함께 측정
- 제공자 API·글 생성·기준 완화 없음. `0.10.0` 후보이며 이후 버전으로 자동 이월하지 않음

### 현재 승인 범위

- 추가 모델 없이 가능한 비교: 순위만 더하는 RRF와 검색 점수의 정규화 결합 비교
- [혼합 검색 결합 함수 연구](https://arxiv.org/abs/2210.11934)의 점수 결합을 참고하되, 다른 자료의 우위를 Hive의 통과 근거로 사용하지 않음
- 현재 실제 FTS 점수와 벡터 거리 점수에 질문별 최소·최대 정규화 적용, 벡터 비중 0.25·0.5·0.75만 사전 지정
- 원본 120문항으로 후보를 선택한 뒤 독립 문항으로 별도 확인. 문항·정답·기준 수정 없음
- 사전 지정 비중 0.25·0.5·0.75의 원본 의미 결과: 86.7%·91.7%·95.0%, 정확 질문은 모두 100%
- 벡터 비중 0.75만 후속 독립 평가 후보로 선택. 이 탐색 뒤 위의 실제 CLI 검증으로 연결, 새 모델 실행 없음

- 초기 생성 보강 후보: 같은 ONNX 모델을 한 프로세스에서 공유하는 제한된 스레드 묶음과 기존 다중 프로세스 비교
- [ONNX Runtime의 동시 실행 계약](https://github.com/microsoft/onnxruntime/blob/main/onnxruntime/core/session/inference_session.h)과 [스레드 설정 안내](https://onnxruntime.ai/docs/performance/tune-performance/threading.html)를 기준으로 CPU만 검사
- 모델·본문 길이·batch·질문·채택 기준 변경 없이 메모리·실제 시간·벡터 결과 동등성 검증 후 적용
- 공유 CPU 세션의 3,072개 탐색: 8개 스레드 33.99초·최대 작업 메모리 2.27GB, 12개 27.21초·2.88GB. 전체 생성 시간으로 환산해 통과 처리 금지
- 이전 다중 프로세스 결과와 일부 벡터의 최대 성분 차이 약 0.00014. 동일 입력 64개는 순차 3회·동시 24회 바이트 일치
- 서로 다른 512개 입력도 순서를 바꾼 동시 32회 실행에서 각 순차 결과와 바이트 일치
- 동시 실행의 같은 입력 반복과 실행 구성 변경의 수치 차이를 구분. 최종 검색 결과·전체 비용 검증 전 출하 방식 확정 금지

1. 고정된 최적화 빌드로 원본 120개·독립 60개 질문의 실제 CLI 재검증
2. 최종 후보의 고유 5만 청크 전체 생성·100개 변경·중단 재개·전체 재생성 동등성
3. 실제 CLI 전체 시간·범위별 디스크 사용량·승인/기밀 경계 재검증
4. Windows x64·macOS arm64·Linux musl의 고정 바이너리 수용

독립 질문 작성자는 기존 질문·모델 점수에 접근하지 않는 별도 검증 역할.
어느 후보도 통과하지 못하면 통과로 표시하거나 기준을 완화하지 않고 미달 원인 보존.
독립 60문항 고정 SHA-256: `e2de7a222f1cb9aad9c5c1f8c90c6890797c3c7b1da896a64a85bb47b8613fcb`.
기존 정답표 감사도 순위·모델 점수와 분리. 감사만으로 원본 질문·정답·기준 변경 금지.

### 평가자료 검토의 한계

- 120개 답변 근거 감사: 타당 84개·복수 정답 5개·필수 조건의 본문 근거 부족 31개
- 후속 독립 검토: 완전한 답변 근거 부족과 검색 관련성 부족의 구분 필요
- 문항을 바꾼 진단용 v2의 원래 검색 기준 대체는 미승인. 원본과 독립 60문항은 그대로 보존
- 단순 버전 문자열의 복수 정답·본문에 없는 숫자는 별도 정정 근거. 문서 간 관계 질문의 일괄 삭제·교체 금지
- 복수 정답의 하나 이상 포함률과 모든 필수 근거의 회수율 구분. 기존 미달을 새 평가셋 통과로 소급 취소 금지

## 원본 실행 자료

- `vector-cli-runtime.json`: 설치·파일 재검사·교체된 Python 거부
- `vector-cli-build.json`: 600개 자료의 중단·작업 파일 오염·복구·재사용
- `vector-consumer-query-results.json`: 최초 실제 CLI의 질문별 비교
- `retrieval-embedding-comparison.json`: 같은 실제 청크의 모델·결합 탐색
- `vector-query-safety.json`: 최신 바이너리의 검색 안전성

위 경로는 모두 격리 시험 폴더 내부. 최초 탐색 기록과 이후 고정 버전의 출시 증거 구분.
