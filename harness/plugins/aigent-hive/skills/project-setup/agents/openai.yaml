interface:
  display_name: "Set up project"
  short_description: "Configure or reuser-setup Aigent Hive for one project."
  default_prompt: "Use $aigent-hive:project-setup to inspect this project and user-setup its local Aigent Hive harness."
policy:
  allow_implicit_invocation: true
