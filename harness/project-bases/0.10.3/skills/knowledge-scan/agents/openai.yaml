interface:
  display_name: "Scan repository knowledge"
  short_description: "(knowledge-scan) Scan one repository or folder and prepare reviewed facts."
  default_prompt: "Use $aigent-hive:knowledge-scan to inventory this directory and prepare reviewed facts."
policy:
  allow_implicit_invocation: false
