interface:
  display_name: "Run multiple goals"
  short_description: "(multi-goal) Execute a bounded goal graph with budgets and terminal verification."
  default_prompt: "Use $aigent-hive:multi-goal for this explicit bounded goal graph."
policy:
  allow_implicit_invocation: false
