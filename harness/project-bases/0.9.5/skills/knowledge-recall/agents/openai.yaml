interface:
  display_name: "Search knowledge (knowledge-recall)"
  short_description: "(knowledge-recall) Before a knowledge-dependent question or task, find only the Hive knowledge that can help with the work at hand. Unregistered folders safely use user-root and shared knowledge."
  default_prompt: "Use $aigent-hive:knowledge-recall to run the single bounded knowledge lookup for this turn."
policy:
  allow_implicit_invocation: false
