schema_version: 1
skills:
  - { name: quick-answer, description: "Answer a self-contained question without inspecting or changing a project.", provided_by: hive, superseded_by_external: [], invocation_intents: [AnswerSimpleQuestion], side_effect_class: none, capabilities: [], availability: implemented }
  - { name: project-setup, description: "Configure Aigent Hive for one project from repository evidence with minimal unresolved questions.", provided_by: hive, superseded_by_external: [], invocation_intents: [SetupHarness], side_effect_class: project-write, capabilities: [filesystem-read, filesystem-write, shell], availability: implemented }
  - { name: code-polish, description: "Clean explicitly selected generated code or wording while preserving behavior and regressions.", provided_by: hive, superseded_by_external: [], invocation_intents: [RunWork], side_effect_class: project-write, capabilities: [filesystem-read, filesystem-write, shell], availability: implemented }
  - { name: ralph-loop, description: "Engineer bounded evidence-gated work loops while leaving task launch to the active host.", provided_by: hive, superseded_by_external: [], invocation_intents: [RunWork], side_effect_class: project-write, capabilities: [filesystem-read, filesystem-write, run-state, shell], availability: implemented }
  - { name: iterative-execution, description: "Execute a bounded persistent criterion loop with host-native receipts and terminal independent verification.", provided_by: hive, superseded_by_external: [], invocation_intents: [RunWork], side_effect_class: project-write, capabilities: [filesystem-read, filesystem-write, run-state, shell, subagents], availability: implemented }
  - { name: team-execution, description: "Coordinate explicit bounded signed lanes with mailbox, barriers, path leases, cancellation, and terminal verification.", provided_by: hive, superseded_by_external: [], invocation_intents: [RunWork], side_effect_class: project-write, capabilities: [filesystem-read, filesystem-write, run-state, shell, subagents], availability: implemented }
  - { name: multi-goal, description: "Execute an explicit bounded goal graph with aggregation, nested budgets, cancellation, evidence, and terminal verification.", provided_by: hive, superseded_by_external: [], invocation_intents: [RunWork], side_effect_class: project-write, capabilities: [filesystem-read, filesystem-write, run-state, shell, subagents], availability: implemented }
  - { name: knowledge-import, description: "Inventory an exact directory and atomically import reviewed repository knowledge.", provided_by: hive, superseded_by_external: [], invocation_intents: [ScanKnowledge], side_effect_class: external-write, capabilities: [filesystem-read, filesystem-write, memory-read, memory-write, shell], availability: implemented }
  - { name: knowledge-maintain, description: "Check, maintain, and rebuild canonical knowledge and its derived index.", provided_by: hive, superseded_by_external: [], invocation_intents: [DeleteKnowledge, LintKnowledge, RebuildKnowledgeIndex, SuppressKnowledge], side_effect_class: project-write, capabilities: [filesystem-read, filesystem-write, memory-read, memory-write, shell], availability: implemented }
  - { name: knowledge-capture, description: "Automatically review every Wiki-enabled turn and record one safe durable claim in every project after installation without requiring project setup.", provided_by: hive, superseded_by_external: [], invocation_intents: [IngestKnowledge], side_effect_class: project-write, capabilities: [filesystem-read, filesystem-write, memory-write, shell], availability: implemented }
  - { name: prompt-refine, description: "Create an approval-ready prompt and stop for exact approval before execution.", provided_by: hive, superseded_by_external: [], invocation_intents: [RefinePrompt], side_effect_class: none, capabilities: [], availability: implemented }
  - { name: research-best-practices, description: "Research current engineering practices from bounded reliable evidence.", provided_by: hive, superseded_by_external: [], invocation_intents: [RunWork], side_effect_class: read-only, capabilities: [filesystem-read, network, shell], availability: implemented }
  - { name: knowledge-recall, description: "Retrieve only the Hive knowledge needed for the current work, with user-root and shared fallback for unregistered projects.", provided_by: hive, superseded_by_external: [], invocation_intents: [AuthorizeConfidentialKnowledge, RetrieveKnowledge], side_effect_class: read-only, capabilities: [filesystem-read, memory-read, shell], availability: implemented }
  - { name: usage-guard, description: "Inspect or change global and project usage safeguards through the signed Hive CLI.", provided_by: hive, superseded_by_external: [], invocation_intents: [RunWork], side_effect_class: project-write, capabilities: [filesystem-read, filesystem-write, run-state, shell], availability: implemented }
  - { name: ship, description: "Prepare focused verified commits using the current repository rules; push only with explicit authority.", provided_by: hive, superseded_by_external: [], invocation_intents: [RunWork], side_effect_class: project-write, capabilities: [filesystem-read, filesystem-write, shell], availability: implemented }
  - { name: amend-directive, description: "Preview and amend user-owned Hive behavior directives without weakening compiled safety boundaries.", provided_by: hive, superseded_by_external: [], invocation_intents: [RunWork], side_effect_class: project-write, capabilities: [filesystem-read, filesystem-write, shell], availability: implemented }
  - { name: user-setup, description: "Configure or reconfigure global Aigent Hive preferences through the signed CLI.", provided_by: hive, superseded_by_external: [], invocation_intents: [SetupHive], side_effect_class: external-write, capabilities: [filesystem-read, filesystem-write, shell], availability: implemented }
  - { name: run-handoff, description: "Update canonical role assignment and handoff records safely.", provided_by: hive, superseded_by_external: [], invocation_intents: [RecordRoleHandoff], side_effect_class: project-write, capabilities: [filesystem-read, filesystem-write, run-state], availability: implemented }
  - { name: project-transition, description: "Migrate a supported Hive project format with backup and validation.", provided_by: hive, superseded_by_external: [], invocation_intents: [MigrateHarness], side_effect_class: project-write, capabilities: [filesystem-read, filesystem-write, shell], availability: implemented }
  - { name: run-resume, description: "Validate a work handoff and read safe resume context.", provided_by: hive, superseded_by_external: [], invocation_intents: [ResumeWork], side_effect_class: read-only, capabilities: [filesystem-read, run-state], availability: implemented }
  - { name: run-checkpoint, description: "Save durable resumable work progress in canonical Markdown.", provided_by: hive, superseded_by_external: [], invocation_intents: [CheckpointRun], side_effect_class: project-write, capabilities: [filesystem-read, filesystem-write, run-state], availability: implemented }
  - { name: knowledge-promote, description: "Promote reviewed project knowledge to global Hive knowledge.", provided_by: hive, superseded_by_external: [], invocation_intents: [PromoteKnowledge], side_effect_class: external-write, capabilities: [filesystem-read, filesystem-write, memory-read, memory-write, shell], availability: implemented }
  - { name: product-update, description: "Stage, verify, and atomically activate a compatible signed Hive update.", provided_by: hive, superseded_by_external: [], invocation_intents: [UpdateHarness], side_effect_class: project-write, capabilities: [filesystem-read, filesystem-write, shell], availability: implemented }
  - { name: project-refresh, description: "Refresh Hive-generated project guidance while preserving local edits.", provided_by: hive, superseded_by_external: [], invocation_intents: [UpdateHarness], side_effect_class: project-write, capabilities: [filesystem-read, filesystem-write, shell], availability: implemented }
  - { name: package-review, description: "Validate a digest-bound package and its signed attestations.", provided_by: hive, superseded_by_external: [], invocation_intents: [PackageJudgeEvidence, VerifyWork], side_effect_class: read-only, capabilities: [filesystem-read, run-state], availability: implemented }
  - { name: custom-subagent-create, description: "Recommend, consent to, and create one Codex and Claude custom-agent profile.", provided_by: hive, superseded_by_external: [], invocation_intents: [RunWork], side_effect_class: project-write, capabilities: [filesystem-read, filesystem-write, run-state, shell, subagents], availability: implemented }
