schema_version: 1
skills:
  - { name: quick-answer, description: "(quick-answer) Answer a self-contained question without inspecting or changing a project.", provided_by: hive, superseded_by_external: [], invocation_intents: [AnswerSimpleQuestion], side_effect_class: none, capabilities: [], availability: implemented }
  - { name: project-setup, description: "(project-setup) Configure Aigent Hive for one project from repository evidence with minimal unresolved questions.", provided_by: hive, superseded_by_external: [], invocation_intents: [SetupHarness], side_effect_class: project-write, capabilities: [filesystem-read, filesystem-write, shell], availability: implemented }
  - { name: code-polish, description: "(code-polish) Clean explicitly selected generated code or wording while preserving behavior and regressions.", provided_by: hive, superseded_by_external: [], invocation_intents: [RunWork], side_effect_class: project-write, capabilities: [filesystem-read, filesystem-write, shell], availability: implemented }
  - { name: verified-workflow, description: "(verified-workflow) Run a bounded evidence-gated workflow with dependency edges, host-native receipts, retry limits, and independent verification.", provided_by: hive, superseded_by_external: [], invocation_intents: [RunWork], side_effect_class: project-write, capabilities: [filesystem-read, filesystem-write, run-state, shell, subagents], availability: implemented }
  - { name: team-execution, description: "(team-execution) Coordinate explicit bounded signed lanes with mailbox, barriers, path leases, cancellation, and terminal verification.", provided_by: hive, superseded_by_external: [], invocation_intents: [RunWork], side_effect_class: project-write, capabilities: [filesystem-read, filesystem-write, run-state, shell, subagents], availability: implemented }
  - { name: multi-goal, description: "(multi-goal) Execute an explicit bounded goal graph with aggregation, nested budgets, cancellation, evidence, and terminal verification.", provided_by: hive, superseded_by_external: [], invocation_intents: [RunWork], side_effect_class: project-write, capabilities: [filesystem-read, filesystem-write, run-state, shell, subagents], availability: implemented }
  - { name: knowledge-import, description: "(knowledge-import) Scan one repository or folder that the user explicitly selected, then import only the reviewed knowledge that is useful beyond that source.", provided_by: hive, superseded_by_external: [], invocation_intents: [ScanKnowledge], side_effect_class: external-write, capabilities: [filesystem-read, filesystem-write, memory-read, memory-write, shell], availability: implemented }
  - { name: knowledge-maintain, description: "(knowledge-maintain) Keep Hive knowledge trustworthy by checking it, rebuilding its search index, or carrying out an explicitly requested cleanup.", provided_by: hive, superseded_by_external: [], invocation_intents: [DeleteKnowledge, LintKnowledge, RebuildKnowledgeIndex, SuppressKnowledge], side_effect_class: project-write, capabilities: [filesystem-read, filesystem-write, memory-read, memory-write, shell], availability: implemented }
  - { name: knowledge-capture, description: "(knowledge-capture) At the end of a Wiki-enabled turn, keep one useful fact, preference, or workflow that will help later work; never save secrets, raw conversations, or uncertain guesses.", provided_by: hive, superseded_by_external: [], invocation_intents: [IngestKnowledge], side_effect_class: project-write, capabilities: [filesystem-read, filesystem-write, memory-write, shell], availability: implemented }
  - { name: prompt-refine, description: "(prompt-refine) Create an approval-ready prompt and stop for exact approval before execution.", provided_by: hive, superseded_by_external: [], invocation_intents: [RefinePrompt], side_effect_class: none, capabilities: [], availability: implemented }
  - { name: research-best-practices, description: "(research-best-practices) Research current engineering practices from bounded reliable evidence.", provided_by: hive, superseded_by_external: [], invocation_intents: [RunWork], side_effect_class: read-only, capabilities: [filesystem-read, network, shell], availability: implemented }
  - { name: knowledge-recall, description: "(knowledge-recall) Before a knowledge-dependent question or task, find only the Hive knowledge that can help with the work at hand. Unregistered folders safely use user-root and shared knowledge.", provided_by: hive, superseded_by_external: [], invocation_intents: [AuthorizeConfidentialKnowledge, RetrieveKnowledge], side_effect_class: read-only, capabilities: [filesystem-read, memory-read, shell], availability: implemented }
  - { name: usage-guard, description: "(usage-guard) Inspect or change global and project usage safeguards through the signed Hive CLI.", provided_by: hive, superseded_by_external: [], invocation_intents: [RunWork], side_effect_class: project-write, capabilities: [filesystem-read, filesystem-write, run-state, shell], availability: implemented }
  - { name: ship, description: "(ship) Prepare focused verified commits using the current repository rules; push only with explicit authority.", provided_by: hive, superseded_by_external: [], invocation_intents: [RunWork], side_effect_class: project-write, capabilities: [filesystem-read, filesystem-write, shell], availability: implemented }
  - { name: amend-directive, description: "(amend-directive) Preview and amend user-owned Hive behavior directives without weakening compiled safety boundaries.", provided_by: hive, superseded_by_external: [], invocation_intents: [RunWork], side_effect_class: project-write, capabilities: [filesystem-read, filesystem-write, shell], availability: implemented }
  - { name: user-setup, description: "(user-setup) Configure or reconfigure global Aigent Hive preferences through the signed CLI.", provided_by: hive, superseded_by_external: [], invocation_intents: [SetupHive], side_effect_class: external-write, capabilities: [filesystem-read, filesystem-write, shell], availability: implemented }
  - { name: run-handoff, description: "(run-handoff) Update canonical role assignment and handoff records safely.", provided_by: hive, superseded_by_external: [], invocation_intents: [RecordRoleHandoff], side_effect_class: project-write, capabilities: [filesystem-read, filesystem-write, run-state], availability: implemented }
  - { name: project-transition, description: "(project-transition) Migrate a supported Hive project format with backup and validation.", provided_by: hive, superseded_by_external: [], invocation_intents: [MigrateHarness], side_effect_class: project-write, capabilities: [filesystem-read, filesystem-write, shell], availability: implemented }
  - { name: run-resume, description: "(run-resume) Validate a work handoff and read safe resume context.", provided_by: hive, superseded_by_external: [], invocation_intents: [ResumeWork], side_effect_class: read-only, capabilities: [filesystem-read, run-state], availability: implemented }
  - { name: run-checkpoint, description: "(run-checkpoint) Save durable resumable work progress in canonical Markdown.", provided_by: hive, superseded_by_external: [], invocation_intents: [CheckpointRun], side_effect_class: project-write, capabilities: [filesystem-read, filesystem-write, run-state], availability: implemented }
  - { name: knowledge-promote, description: "(knowledge-promote) Share a reviewed fact, preference, or workflow from one project when it can genuinely help the user's other work.", provided_by: hive, superseded_by_external: [], invocation_intents: [PromoteKnowledge], side_effect_class: external-write, capabilities: [filesystem-read, filesystem-write, memory-read, memory-write, shell], availability: implemented }
  - { name: product-update, description: "(product-update) Stage, verify, and atomically activate a compatible signed Hive update.", provided_by: hive, superseded_by_external: [], invocation_intents: [UpdateHarness], side_effect_class: project-write, capabilities: [filesystem-read, filesystem-write, shell], availability: implemented }
  - { name: project-refresh, description: "(project-refresh) Refresh Hive-generated project guidance while preserving local edits.", provided_by: hive, superseded_by_external: [], invocation_intents: [UpdateHarness], side_effect_class: project-write, capabilities: [filesystem-read, filesystem-write, shell], availability: implemented }
  - { name: package-review, description: "(package-review) Validate a digest-bound package and its signed attestations.", provided_by: hive, superseded_by_external: [], invocation_intents: [PackageJudgeEvidence, VerifyWork], side_effect_class: read-only, capabilities: [filesystem-read, run-state], availability: implemented }
  - { name: custom-subagent-create, description: "(custom-subagent-create) Recommend, consent to, and create one Codex and Claude custom-agent profile.", provided_by: hive, superseded_by_external: [], invocation_intents: [RunWork], side_effect_class: project-write, capabilities: [filesystem-read, filesystem-write, run-state, shell, subagents], availability: implemented }
