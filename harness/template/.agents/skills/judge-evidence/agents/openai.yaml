interface:
  display_name: "Judge evidence"
  short_description: "(judge-evidence) Verify a work package and its signed attestations."
  default_prompt: "Use $aigent-hive:judge-evidence to validate this clean-context Judge package and provenance chain."
policy:
  allow_implicit_invocation: false
