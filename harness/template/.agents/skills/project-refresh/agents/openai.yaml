interface:
  display_name: "Upgrade project"
  short_description: "(project-refresh) Preview or update this project's Hive guidance on request, preserving local edits. Not global installation or application updates."
  default_prompt: "Use $aigent-hive:project-refresh to inspect and update this project's Hive guidance, preserving local edits and validating the result."
policy:
  allow_implicit_invocation: false
