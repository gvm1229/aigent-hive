interface:
  display_name: "AI Slop Cleaner"
  short_description: "Clean code slop without behavior regressions"
  default_prompt: "Use $ai-slop-cleaner to clean these changed files while preserving behavior."
policy:
  allow_implicit_invocation: false
